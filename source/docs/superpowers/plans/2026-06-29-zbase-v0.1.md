# ZBase v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零交付 ZBase v0.1 —— 一个跨平台 (Windows/Linux/macOS) 的 C/C++ 通用工具动态库，包含 CMake 构建骨架、6 个核心模块 (error/string/time/file/perf/log)、C ABI 导出层、header-only C++ 包装层、GoogleTest 单元测试和端到端示例。

**Architecture:** 五层架构 —— 平台适配层 (UTF-8↔UTF-16、Win32/POSIX 文件时间原语) → 核心模块 (C++20 实现) → C ABI 导出层 (统一 `ZBASE_C_API_BEGIN/END` 异常包裹) → C++ RAII 包装层 (header-only) → 公共头 (`include/zbase/*.h`、`include/zbase++/*.hpp`)。模块依赖自下而上：error → string → time → file → perf → log。

**Tech Stack:** C++20 / CMake 3.20+ / Ninja / GoogleTest 1.17.0 / MSVC + MinGW + GCC + Clang / UTF-8 外部接口 + UTF-16 Win32 内部。

**Source Spec:** [architecture.md](file:///c:/Repos/ZBase/source/docs/architecture.md) + [phase-1-working-file.md](file:///c:/Repos/ZBase/source/docs/phase-1-working-file.md)。

---

## File Structure Map

```
source/
├── CMakeLists.txt                     # 顶层 CMake 入口
├── cmake/
│   ├── zbase_options.cmake            # 选项定义（BUILD_SHARED_LIBS/ZBASE_BUILD_TESTS）
│   ├── zbase_compile_options.cmake    # 跨平台编译选项（/utf-8 /W4 /permissive- -fvisibility=hidden）
│   └── zbase_install.cmake            # install 规则 + export targets
├── include/
│   ├── zbase/
│   │   ├── zbase.h                    # 公共版本宏 + ZBASE_API/ZBASE_LOCAL 宏定义
│   │   ├── error.h                    # 错误码枚举 + z_error_msg
│   │   ├── string.h                   # 字符串工具 C ABI
│   │   ├── time.h                     # 时间工具 C ABI
│   │   ├── file.h                     # 文件工具 C ABI
│   │   ├── perf.h                     # 性能统计 C ABI
│   │   ├── log.h                      # 日志 C ABI
│   │   └── internal/
│   │       ├── c_api_guard.hpp        # ZBASE_C_API_BEGIN/END 宏（内部头，不发布）
│   │       └── platform.hpp           # 内部平台声明
│   └── zbase++/
│       ├── error.hpp                  # C++ 异常包装
│       ├── string.hpp                 # std::string 风格
│       ├── time.hpp
│       ├── file.hpp                   # RAII FileReader/FileWriter
│       ├── perf.hpp                   # RAII PerfScope
│       └── log.hpp
├── src/
│   ├── platform/
│   │   ├── encoding.hpp              # UTF-8↔UTF-16 内部接口
│   │   ├── encoding.cpp
│   │   ├── file_platform.hpp          # 文件操作平台原语
│   │   ├── file_platform.cpp
│   │   ├── time_platform.hpp          # 时间原语
│   │   └── time_platform.cpp
│   ├── core/
│   │   ├── error/
│   │   │   └── error.cpp              # z_error_msg 实现
│   │   ├── string/
│   │   │   └── string.cpp            # split/join/replace/trim/...
│   │   ├── time/
│   │   │   └── time.cpp
│   │   ├── file/
│   │   │   ├── file_reader.cpp
│   │   │   ├── file_writer.cpp
│   │   │   └── dir_iter.cpp
│   │   ├── perf/
│   │   │   └── perf.cpp
│   │   └── log/
│   │       ├── log.cpp
│   │       └── console.cpp            # Windows 控制台 UTF-8 save/restore
│   └── c_api/
│       ├── error_c.cpp
│       ├── string_c.cpp
│       ├── time_c.cpp
│       ├── file_c.cpp
│       ├── perf_c.cpp
│       └── log_c.cpp
├── tests/
│   ├── platform/
│   │   ├── test_encoding.cpp
│   │   ├── test_file_platform.cpp
│   │   └── test_time_platform.cpp
│   ├── core/
│   │   ├── test_error.cpp
│   │   ├── test_string.cpp
│   │   ├── test_time.cpp
│   │   ├── test_file.cpp
│   │   ├── test_perf.cpp
│   │   └── test_log.cpp
│   └── integration/
│       └── test_demo.cpp
├── examples/
│   └── demo.cpp
└── thirdparty/googletest-1.17.0/      # 已存在
```

---

## Cross-Task Conventions

以下约定在所有 Task 中复用，避免在每个 Task 重复样板代码。

### Conventions 1: 文件头 doxygen 注释

每个 `.h` / `.hpp` / `.cpp` 顶部必须有：

```cpp
/**
 * @file file.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件操作 C ABI 接口
 * @details 提供跨平台的文本文件读写、目录遍历等能力。
 */
```

### Conventions 2: 函数 doxygen 中文注释

所有头文件中的函数都需要：
```cpp
/**
 * @brief 读取文本文件全部内容
 * @param path 文件路径（UTF-8）
 * @param out_buf 输出缓冲区（库内申请，调 z_file_free 释放）
 * @param out_len 输出长度
 * @return Z_OK 成功；Z_ERR_* 失败
 */
```

### Conventions 3: 成员变量注释
```cpp
char* buf_;   ///< 缓冲区指针
size_t len_;  ///< 缓冲区长度
```

### Conventions 4: commit message 格式

`feat:` 新功能 / `fix:` bug 修复 / `test:` 测试 / `build:` 构建 / `docs:` 文档 / `refactor:` 重构 / `chore:` 杂项。

---

## Task 0: CMake 骨架与跨平台编译选项

**Files:**
- Create: `c:\Repos\ZBase\source\CMakeLists.txt`
- Create: `c:\Repos\ZBase\source\cmake\zbase_options.cmake`
- Create: `c:\Repos\ZBase\source\cmake\zbase_compile_options.cmake`
- Create: `c:\Repos\ZBase\source\cmake\zbase_install.cmake`
- Create: `c:\Repos\ZBase\source\.gitignore`
- Create: `c:\Repos\ZBase\source\src\platform\dummy.cpp` (空 TU 验证链接链路)

**Goal:** 四套工具链 (MSVC / MinGW / GCC / Clang) 都能 `configure + build` 一个空目标，所有跨平台编译选项生效，无警告。

- [ ] **Step 0.1: 写顶层 CMakeLists.txt**

Create `c:\Repos\ZBase\source\CMakeLists.txt`:

```cmake
cmake_minimum_required(VERSION 3.20)

project(zbase
    VERSION 0.1.0
    DESCRIPTION "ZBase 个人跨平台 C/C++ 通用工具动态库"
    LANGUAGES C CXX
)

# C++20 标准（不用 23 是为了 MinGW 兼容性）
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)

# 默认构建为动态库，可由 -DBUILD_SHARED_LIBS=OFF 切换为静态
option(BUILD_SHARED_LIBS "构建为动态库（OFF 则静态）" ON)
option(ZBASE_BUILD_TESTS "构建单元测试" ON)
option(ZBASE_BUILD_EXAMPLES "构建示例" ON)

# 加载各模块
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
include(zbase_options)
include(zbase_compile_options)

# 源码查找：禁止硬编码，必须用 file(GLOB_RECURSE)
file(GLOB_RECURSE ZBASE_SOURCES CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/src/platform/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/core/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/c_api/*.cpp"
)
file(GLOB_RECURSE ZBASE_PUBLIC_HEADERS CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/include/zbase/*.h"
)

# 主库目标
add_library(zbase ${ZBASE_SOURCES})
add_library(zbase::zbase ALIAS zbase)

target_include_directories(zbase
    PUBLIC
        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
        $<INSTALL_INTERFACE:include>
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_compile_features(zbase PUBLIC cxx_std_20)

# 应用编译选项
zbase_apply_compile_options(zbase)

# 版本与 soname
set_target_properties(zbase PROPERTIES
    VERSION ${PROJECT_VERSION}
    SOVERSION 0
)

# install 规则
include(zbase_install)
zbase_setup_install(zbase)

# 测试
if(ZBASE_BUILD_TESTS)
    enable_testing()
    add_subdirectory(thirdparty/googletest-1.17.0)
    add_subdirectory(tests)
endif()

# 示例
if(ZBASE_BUILD_EXAMPLES)
    add_subdirectory(examples)
endif()
```

- [ ] **Step 0.2: 写 cmake/zbase_options.cmake**

Create `c:\Repos\ZBase\source\cmake\zbase_options.cmake`:

```cmake
# ZBase 选项定义模块
# 控制静态/动态库的 CRT 链接方式（详见 architecture.md §12.4）

if(BUILD_SHARED_LIBS)
    set(ZBASE_MSVC_RUNTIME_LIBRARY "MultiThreadedDLL" CACHE STRING "MSVC 运行时库" FORCE)
    set(ZBASE_STATIC_DEFINE OFF)
else()
    set(ZBASE_MSVC_RUNTIME_LIBRARY "MultiThreaded" CACHE STRING "MSVC 运行时库" FORCE)
    set(ZBASE_STATIC_DEFINE ON)
endif()

# 强制 CMake 用一致的 CRT（避免 CRT 混用，致命风险）
set(CMAKE_MSVC_RUNTIME_LIBRARY ${ZBASE_MSVC_RUNTIME_LIBRARY} CACHE STRING "" FORCE)

message(STATUS "ZBase 构建模式: ${ZBASE_MSVC_RUNTIME_LIBRARY} (BUILD_SHARED_LIBS=${BUILD_SHARED_LIBS})")
```

- [ ] **Step 0.3: 写 cmake/zbase_compile_options.cmake**

Create `c:\Repos\ZBase\source\cmake\zbase_compile_options.cmake`:

```cmake
# ZBase 跨平台编译选项
# 提供 zbase_apply_compile_options(target) 函数

function(zbase_apply_compile_options target)
    # 公共定义：控制 ZBASE_API 宏展开
    if(ZBASE_STATIC_DEFINE)
        target_compile_definitions(${target} PUBLIC ZBASE_STATIC_DEFINE)
    endif()

    if(MSVC)
        # /utf-8 同时设源码和执行字符集
        target_compile_options(${target} PRIVATE /utf-8 /W4 /permissive- /WX-)
        # 多处理器编译加速
        target_compile_options(${target} PRIVATE /MP)
    elseif(CMAKE_CXX_COMPILER_ID STREQUAL "GNU" OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
        # GCC/Clang
        target_compile_options(${target} PRIVATE
            -Wall -Wextra -Wpedantic
            -finput-charset=UTF-8 -fexec-charset=UTF-8
            -fvisibility=hidden -fvisibility-inlines-hidden
        )
        # 静态库模式下不需要 -fPIC
        if(BUILD_SHARED_LIBS)
            set_target_properties(${target} PROPERTIES POSITION_INDEPENDENT_CODE ON)
        endif()
    endif()

    # Windows 平台额外定义
    if(WIN32)
        target_compile_definitions(${target} PRIVATE
            WIN32_LEAN_AND_MEAN
            NOMINMAX
            _CRT_SECURE_NO_WARNINGS
            UNICODE
            _UNICODE
        )
    endif()
endfunction()
```

- [ ] **Step 0.4: 写 cmake/zbase_install.cmake**

Create `c:\Repos\ZBase\source\cmake\zbase_install.cmake`:

```cmake
# ZBase install 规则
# 详见 architecture.md §14.2

include(GNUInstallDirs)

function(zbase_setup_install target)
    # 头文件
    install(DIRECTORY ${CMAKE_SOURCE_DIR}/include/
            DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
            FILES_MATCHING PATTERN "*.h" PATTERN "*.hpp")

    # 库 + 运行时
    install(TARGETS ${target}
        EXPORT ZBaseTargets
        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
        ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
        INCLUDES DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
    )

    # export 配置
    install(EXPORT ZBaseTargets
        FILE ZBaseTargets.cmake
        NAMESPACE ZBase::
        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/ZBase
    )

    # 版本配置文件
    include(CMakePackageConfigHelpers)
    write_basic_package_version_file(
        "${CMAKE_CURRENT_BINARY_DIR}/ZBaseConfigVersion.cmake"
        VERSION ${PROJECT_VERSION}
        COMPATIBILITY SameMajorVersion
    )
    configure_package_config_file(
        "${CMAKE_SOURCE_DIR}/cmake/ZBaseConfig.cmake.in"
        "${CMAKE_CURRENT_BINARY_DIR}/ZBaseConfig.cmake"
        INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/ZBase
    )
    install(FILES
        "${CMAKE_CURRENT_BINARY_DIR}/ZBaseConfig.cmake"
        "${CMAKE_CURRENT_BINARY_DIR}/ZBaseConfigVersion.cmake"
        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/ZBase
    )

    # RPATH（详见 architecture.md §14.3）
    if(APPLE)
        set_target_properties(${target} PROPERTIES
            INSTALL_NAME_DIR "@rpath"
            MACOSX_RPATH ON
        )
    elseif(UNIX AND NOT APPLE)
        set_target_properties(${target} PROPERTIES
            INSTALL_RPATH "$ORIGIN"
            BUILD_RPATH "$ORIGIN"
        )
    endif()
endfunction()
```

- [ ] **Step 0.5: 写 cmake/ZBaseConfig.cmake.in**

Create `c:\Repos\ZBase\source\cmake\ZBaseConfig.cmake.in`:

```cmake
@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/ZBaseTargets.cmake")

check_required_components(ZBase)
```

- [ ] **Step 0.6: 写 .gitignore**

Create `c:\Repos\ZBase\source\.gitignore`:

```
build/
build-*/
*.obj
*.o
*.so
*.so.*
*.dll
*.dylib
*.lib
*.exp
*.pdb
*.exe
.vs/
.vscode/
CMakeUserPresets.json
```

- [ ] **Step 0.7: 写空 TU 验证链接**

Create `c:\Repos\ZBase\source\src\platform\dummy.cpp`:

```cpp
/**
 * @file dummy.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 占位空翻译单元，仅用于 Step 0 验证 CMake 链路
 * @details Task 1 起此文件可删除。
 */
namespace zbase {
namespace platform {
inline void dummy_link_anchor() {}
}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 0.8: 临时注释 tests/ 和 examples/ 子目录**

CMakeLists 中 `add_subdirectory(tests)` 和 `add_subdirectory(examples)` 要求这两个目录有 CMakeLists.txt。Task 0 阶段先把它们建空：

Create `c:\Repos\ZBase\source\tests\CMakeLists.txt`:
```cmake
# Task 0 占位，后续 Task 会填入 GLOB_RECURSE
```

Create `c:\Repos\ZBase\source\examples\CMakeLists.txt`:
```cmake
# Task 0 占位
```

- [ ] **Step 0.9: 配置 + 构建（MSVC）**

```powershell
cd c:\Repos\ZBase\source
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl
cmake --build build
```

Expected: `zbase.dll` + `zbase.lib` 生成于 `build/`，无错误无警告。

- [ ] **Step 0.10: 配置 + 构建（MinGW）**

```powershell
cd c:\Repos\ZBase\source
cmake -B build-mingw -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++
cmake --build build-mingw
```

Expected: `libzbase.dll` + `libzbase.dll.a` 生成，无错误。

- [ ] **Step 0.11: Commit**

```powershell
cd c:\Repos\ZBase
git add source/CMakeLists.txt source/cmake/ source/.gitignore source/src/platform/dummy.cpp source/tests/CMakeLists.txt source/examples/CMakeLists.txt
git commit -m "build: 搭建 ZBase v0.1 CMake 跨平台骨架"
```

**验收标准：**
- MSVC 与 MinGW 两套工具链都能成功 configure + build
- `build/` 目录下生成动态库产物
- `cmake --build build --target install` 可以 install 到本地（DESTDIR 测试）

---

## Task 1: 平台层 encoding 模块（UTF-8↔UTF-16）

**Files:**
- Create: `c:\Repos\ZBase\source\src\platform\encoding.hpp`
- Create: `c:\Repos\ZBase\source\src\platform\encoding.cpp`
- Create: `c:\Repos\ZBase\source\tests\platform\test_encoding.cpp`
- Modify: `c:\Repos\ZBase\source\tests\CMakeLists.txt`

**Goal:** 提供 UTF-8 ↔ UTF-16 转换的内部接口（不导出 C ABI），Windows 走 `MultiByteToWideChar` / `WideCharToMultiByte`，POSIX 走 `iconv` 或纯算法实现。所有后续模块依赖此层。

- [ ] **Step 1.1: 写测试用例**

Create `c:\Repos\ZBase\source\tests\platform\test_encoding.cpp`:

```cpp
/**
 * @file test_encoding.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 平台层 encoding 单元测试
 */

#include <gtest/gtest.h>
#include "platform/encoding.hpp"

#include <string>

TEST(Encoding, Utf8ToUtf16_Empty) {
    std::u16string out;
    EXPECT_EQ(zbase::platform::Utf8ToUtf16("", out), Z_OK);
    EXPECT_TRUE(out.empty());
}

TEST(Encoding, Utf8ToUtf16_Ascii) {
    std::u16string out;
    EXPECT_EQ(zbase::platform::Utf8ToUtf16("hello", out), Z_OK);
    EXPECT_EQ(out.size(), 5u);
    EXPECT_EQ(out[0], u'h');
}

TEST(Encoding, Utf8ToUtf16_Chinese) {
    std::u16string out;
    EXPECT_EQ(zbase::platform::Utf8ToUtf16("你好,世界", out), Z_OK);
    EXPECT_EQ(out.size(), 5u);  // 4 个汉字 + 1 个逗号
    EXPECT_EQ(out[0], u'你');
}

TEST(Encoding, Utf16ToUtf8_RoundTrip) {
    std::string original = "测试 UTF-8 ↔ UTF-16 转换roundtrip";
    std::u16string mid;
    ASSERT_EQ(zbase::platform::Utf8ToUtf16(original, mid), Z_OK);
    std::string back;
    ASSERT_EQ(zbase::platform::Utf16ToUtf8(mid, back), Z_OK);
    EXPECT_EQ(original, back);
}

TEST(Encoding, Utf8ToUtf16_InvalidSequence) {
    std::u16string out;
    // 0xFF 是非法 UTF-8 起始字节
    EXPECT_NE(zbase::platform::Utf8ToUtf16(std::string("\xFF\xFE"), out), Z_OK);
}
```

- [ ] **Step 1.2: 修改 tests/CMakeLists.txt 注册测试**

Replace `c:\Repos\ZBase\source\tests\CMakeLists.txt`:

```cmake
file(GLOB_RECURSE ZBASE_TEST_SOURCES CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp"
)

add_executable(zbase_tests ${ZBASE_TEST_SOURCES})
target_link_libraries(zbase_tests PRIVATE zbase gtest gtest_main)

target_include_directories(zbase_tests PRIVATE
    ${CMAKE_SOURCE_DIR}/src
)

include(GoogleTest)
gtest_discover_tests(zbase_tests)
```

- [ ] **Step 1.3: 跑测试确认失败**

```powershell
cd c:\Repos\ZBase\source
cmake --build build
ctest --test-dir build --output-on-failure
```

Expected: 编译失败，找不到 `platform/encoding.hpp`。

- [ ] **Step 1.4: 写 encoding.hpp 接口**

Create `c:\Repos\ZBase\source\src\platform\encoding.hpp`:

```cpp
/**
 * @file encoding.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief UTF-8 ↔ UTF-16 转换内部接口（平台层，不对外导出）
 * @details Windows 走 MultiByteToWideChar；其他平台走纯算法实现（RFC 3629）。
 */

#pragma once

#include <cstdint>
#include <string>

#include "zbase/error.h"

namespace zbase {
namespace platform {

/**
 * @brief UTF-8 字符串转 UTF-16
 * @param in 输入 UTF-8 字符串
 * @param out 输出 UTF-16 字符串（库内追加，不清空）
 * @return Z_OK 成功；Z_ERR_ENCODING 输入非法 UTF-8；Z_ERR_NOMEM 内存不足
 */
int Utf8ToUtf16(const std::string& in, std::u16string& out);

/**
 * @brief UTF-16 字符串转 UTF-8
 * @param in 输入 UTF-16 字符串
 * @param out 输出 UTF-8 字符串
 * @return Z_OK 成功；Z_ERR_ENCODING 输入非法；Z_ERR_NOMEM 内存不足
 */
int Utf16ToUtf8(const std::u16string& in, std::string& out);

/**
 * @brief UTF-8 字符串转 std::wstring（Windows 下为 UTF-16，POSIX 下为 UTF-32）
 * @param in 输入 UTF-8
 * @param out 输出 wstring
 * @return Z_OK 成功；否则错误码
 * @note Windows 上等价于 Utf8ToUtf16；POSIX 上走 UTF-32 路径
 */
int Utf8ToWString(const std::string& in, std::wstring& out);

/**
 * @brief std::wstring 转 UTF-8（Utf8ToWString 的逆操作）
 */
int WStringToUtf8(const std::wstring& in, std::string& out);

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 1.5: 写 encoding.cpp 实现**

Create `c:\Repos\ZBase\source\src\platform\encoding.cpp`:

```cpp
/**
 * @file encoding.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief UTF-8 ↔ UTF-16 转换实现
 */

#include "platform/encoding.hpp"

#ifdef _WIN32
#include <Windows.h>
#else
#include <cstdint>
#endif

namespace zbase {
namespace platform {

#ifdef _WIN32
// Windows: 走 MultiByteToWideChar / WideCharToMultiByte

int Utf8ToUtf16(const std::string& in, std::u16string& out) {
    if (in.empty()) { out.clear(); return Z_OK; }
    int len = MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS,
                                  in.data(), static_cast<int>(in.size()),
                                  nullptr, 0);
    if (len == 0) {
        return (GetLastError() == ERROR_NO_UNICODE_TRANSLATION)
               ? Z_ERR_ENCODING : Z_ERR_UNKNOWN;
    }
    out.resize(static_cast<size_t>(len));
    MultiByteToWideChar(CP_UTF8, MB_ERR_INVALID_CHARS,
                        in.data(), static_cast<int>(in.size()),
                        reinterpret_cast<LPWSTR>(&out[0]), len);
    return Z_OK;
}

int Utf16ToUtf8(const std::u16string& in, std::string& out) {
    if (in.empty()) { out.clear(); return Z_OK; }
    int len = WideCharToMultiByte(CP_UTF8, WC_ERR_INVALID_CHARS,
                                  reinterpret_cast<LPCWSTR>(in.data()),
                                  static_cast<int>(in.size()),
                                  nullptr, 0, nullptr, nullptr);
    if (len == 0) {
        return (GetLastError() == ERROR_NO_UNICODE_TRANSLATION)
               ? Z_ERR_ENCODING : Z_ERR_UNKNOWN;
    }
    out.resize(static_cast<size_t>(len));
    WideCharToMultiByte(CP_UTF8, WC_ERR_INVALID_CHARS,
                        reinterpret_cast<LPCWSTR>(in.data()),
                        static_cast<int>(in.size()),
                        &out[0], len, nullptr, nullptr);
    return Z_OK;
}

int Utf8ToWString(const std::string& in, std::wstring& out) {
    std::u16string u16;
    int err = Utf8ToUtf16(in, u16);
    if (err != Z_OK) return err;
    out.assign(reinterpret_cast<const wchar_t*>(u16.data()), u16.size());
    return Z_OK;
}

int WStringToUtf8(const std::wstring& in, std::string& out) {
    return Utf16ToUtf8(
        std::u16string(reinterpret_cast<const char16_t*>(in.data()), in.size()),
        out);
}

#else
// POSIX: 纯算法实现（RFC 3629 UTF-8 + UCS-2/BMP-only UTF-16）
// 注：v0.1 仅支持 BMP（U+0000 ~ U+FFFF），代理对留 v0.2 补

namespace {

// 解码一个 UTF-8 字符，返回码点 + 消耗字节数；失败返回 -1
int32_t DecodeUtf8(const char* s, size_t len, size_t& consumed) {
    if (len == 0) return -1;
    uint8_t c0 = static_cast<uint8_t>(s[0]);
    if (c0 < 0x80) { consumed = 1; return c0; }
    if ((c0 & 0xE0) == 0xC0) {
        if (len < 2) return -1;
        uint8_t c1 = static_cast<uint8_t>(s[1]);
        if ((c1 & 0xC0) != 0x80) return -1;
        uint32_t cp = ((c0 & 0x1F) << 6) | (c1 & 0x3F);
        if (cp < 0x80) return -1;  // 过长编码
        consumed = 2; return cp;
    }
    if ((c0 & 0xF0) == 0xE0) {
        if (len < 3) return -1;
        uint8_t c1 = static_cast<uint8_t>(s[1]);
        uint8_t c2 = static_cast<uint8_t>(s[2]);
        if ((c1 & 0xC0) != 0x80 || (c2 & 0xC0) != 0x80) return -1;
        uint32_t cp = ((c0 & 0x0F) << 12) | ((c1 & 0x3F) << 6) | (c2 & 0x3F);
        if (cp < 0x800) return -1;
        if (cp >= 0xD800 && cp <= 0xDFFF) return -1;  // 代理对区
        consumed = 3; return cp;
    }
    // 4 字节序列超出 BMP，v0.1 不支持，返回错误
    return -1;
}

}  // namespace

int Utf8ToUtf16(const std::string& in, std::u16string& out) {
    out.clear();
    size_t i = 0;
    while (i < in.size()) {
        size_t consumed = 0;
        int32_t cp = DecodeUtf8(in.data() + i, in.size() - i, consumed);
        if (cp < 0) return Z_ERR_ENCODING;
        if (cp > 0xFFFF) return Z_ERR_ENCODING;  // v0.1 不支持代理对
        out.push_back(static_cast<char16_t>(cp));
        i += consumed;
    }
    return Z_OK;
}

int Utf16ToUtf8(const std::u16string& in, std::string& out) {
    out.clear();
    for (char16_t c : in) {
        uint32_t cp = static_cast<uint32_t>(c);
        if (cp >= 0xD800 && cp <= 0xDFFF) return Z_ERR_ENCODING;
        if (cp < 0x80) {
            out.push_back(static_cast<char>(cp));
        } else if (cp < 0x800) {
            out.push_back(static_cast<char>(0xC0 | (cp >> 6)));
            out.push_back(static_cast<char>(0x80 | (cp & 0x3F)));
        } else {
            out.push_back(static_cast<char>(0xE0 | (cp >> 12)));
            out.push_back(static_cast<char>(0x80 | ((cp >> 6) & 0x3F)));
            out.push_back(static_cast<char>(0x80 | (cp & 0x3F)));
        }
    }
    return Z_OK;
}

int Utf8ToWString(const std::string& in, std::wstring& out) {
    // POSIX: wchar_t 是 UTF-32
    out.clear();
    size_t i = 0;
    while (i < in.size()) {
        size_t consumed = 0;
        int32_t cp = DecodeUtf8(in.data() + i, in.size() - i, consumed);
        if (cp < 0) return Z_ERR_ENCODING;
        out.push_back(static_cast<wchar_t>(cp));
        i += consumed;
    }
    return Z_OK;
}

int WStringToUtf8(const std::wstring& in, std::string& out) {
    out.clear();
    for (wchar_t wc : in) {
        uint32_t cp = static_cast<uint32_t>(wc);
        if (cp < 0x80) {
            out.push_back(static_cast<char>(cp));
        } else if (cp < 0x800) {
            out.push_back(static_cast<char>(0xC0 | (cp >> 6)));
            out.push_back(static_cast<char>(0x80 | (cp & 0x3F)));
        } else if (cp < 0x10000) {
            out.push_back(static_cast<char>(0xE0 | (cp >> 12)));
            out.push_back(static_cast<char>(0x80 | ((cp >> 6) & 0x3F)));
            out.push_back(static_cast<char>(0x80 | (cp & 0x3F)));
        } else {
            out.push_back(static_cast<char>(0xF0 | (cp >> 18)));
            out.push_back(static_cast<char>(0x80 | ((cp >> 12) & 0x3F)));
            out.push_back(static_cast<char>(0x80 | ((cp >> 6) & 0x3F)));
            out.push_back(static_cast<char>(0x80 | (cp & 0x3F)));
        }
    }
    return Z_OK;
}

#endif

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 1.6: 跑测试确认通过**

```powershell
cmake --build build
ctest --test-dir build -R Encoding --output-on-failure
```

Expected: 5 个 Encoding 测试全 PASS。

- [ ] **Step 1.7: Commit**

```powershell
git add source/src/platform/encoding.hpp source/src/platform/encoding.cpp source/tests/platform/test_encoding.cpp source/tests/CMakeLists.txt
git commit -m "feat(platform): 实现 UTF-8↔UTF-16 转换及单元测试"
```

**验收标准：**
- `ctest -R Encoding` 5 个用例全绿
- MinGW 下同样通过（验证 `MultiByteToWideChar` 路径与算法路径都正确）

---

## Task 2: error 模块（错误码枚举 + z_error_msg）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\zbase.h` (公共宏 + 版本)
- Create: `c:\Repos\ZBase\source\include\zbase\error.h`
- Create: `c:\Repos\ZBase\source\src\core\error\error.cpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_error.cpp`

**Goal:** 定义 9 个错误码的 `typedef enum` + 中文描述表 + `static_assert(sizeof(z_error_t)==sizeof(int))`。

- [ ] **Step 2.1: 写 zbase.h 公共宏**

Create `c:\Repos\ZBase\source\include\zbase\zbase.h`:

```c
/**
 * @file zbase.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief ZBase 公共头：版本宏 + 符号导出宏
 * @details 所有公开头文件应在本文件之前通过 #include "zbase/zbase.h" 引入。
 */

#ifndef ZBASE_ZBASE_H_
#define ZBASE_ZBASE_H_

#ifdef __cplusplus
extern "C" {
#endif

/* 版本号宏 */
#define ZBASE_VERSION_MAJOR 0
#define ZBASE_VERSION_MINOR 1
#define ZBASE_VERSION_PATCH 0
#define ZBASE_VERSION_STRING "0.1.0"

/**
 * @brief 获取库版本字符串
 * @return 形如 "0.1.0" 的静态字符串
 */
const char* z_version(void);

/* 符号导出宏（详见 architecture.md §12.1） */
#if defined(ZBASE_STATIC_DEFINE)
  #define ZBASE_API
  #define ZBASE_LOCAL
#elif defined(_WIN32)
  #if defined(ZBASE_EXPORTS)
    #define ZBASE_API __declspec(dllexport)
  #else
    #define ZBASE_API __declspec(dllimport)
  #endif
  #define ZBASE_LOCAL
#elif defined(__GNUC__) || defined(__clang__)
  #define ZBASE_API __attribute__((visibility("default")))
  #define ZBASE_LOCAL  __attribute__((visibility("hidden")))
#else
  #define ZBASE_API
  #define ZBASE_LOCAL
#endif

/* 弃用宏（详见 architecture.md §13.5） */
#if defined(__cplusplus) && __cplusplus >= 201402L
  #define ZBASE_DEPRECATED(msg) [[deprecated(msg)]]
#elif defined(_MSC_VER)
  #define ZBASE_DEPRECATED(msg) __declspec(deprecated(msg))
#elif defined(__GNUC__) || defined(__clang__)
  #define ZBASE_DEPRECATED(msg) __attribute__((deprecated(msg)))
#else
  #define ZBASE_DEPRECATED(msg)
#endif

#ifdef __cplusplus
}
#endif

#endif  // ZBASE_ZBASE_H_
```

- [ ] **Step 2.2: 写 error.h**

Create `c:\Repos\ZBase\source\include\zbase\error.h`:

```c
/**
 * @file error.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 错误码体系 C ABI
 * @details 详见 architecture.md §9。所有 C ABI 函数返回 int，0 表示成功，负数表示错误。
 */

#ifndef ZBASE_ERROR_H_
#define ZBASE_ERROR_H_

#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief 错误码枚举
 * @note sizeof(z_error_t) 必须等于 sizeof(int)，详见文件末 static_assert
 */
typedef enum {
    Z_OK               = 0,   ///< 成功
    Z_ERR_INVALID_ARG  = -1,  ///< 参数非法
    Z_ERR_NOT_FOUND    = -2,  ///< 找不到
    Z_ERR_PERMISSION   = -3,  ///< 权限不足
    Z_ERR_IO          = -4,   ///< IO 错误
    Z_ERR_ENCODING    = -5,   ///< 编码错误
    Z_ERR_NOMEM       = -6,   ///< 内存不足
    Z_ERR_OVERFLOW    = -7,   ///< 溢出
    Z_ERR_UNSUPPORTED = -8,   ///< 不支持
    Z_ERR_UNKNOWN     = -99,  ///< 未知错误
} z_error_t;

/**
 * @brief 获取错误码对应的中文描述
 * @param err 错误码
 * @return 静态字符串指针，不需要释放
 */
ZBASE_API const char* z_error_msg(int err);

#ifdef __cplusplus
}  // extern "C"

#include <type_traits>
static_assert(sizeof(z_error_t) == sizeof(int),
              "z_error_t 必须是 int 大小（详见 architecture.md §9）");
#else
#include <assert.h>
_Static_assert(sizeof(z_error_t) == sizeof(int),
               "z_error_t 必须是 int 大小");
#endif

#endif  // ZBASE_ERROR_H_
```

- [ ] **Step 2.3: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_error.cpp`:

```cpp
/**
 * @file test_error.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief error 模块单元测试
 */

#include <gtest/gtest.h>
#include "zbase/error.h"

TEST(Error, OkIsZero) {
    EXPECT_EQ(Z_OK, 0);
}

TEST(Error, AllCodesNegativeExceptOk) {
    EXPECT_LT(Z_ERR_INVALID_ARG, 0);
    EXPECT_LT(Z_ERR_NOT_FOUND, 0);
    EXPECT_LT(Z_ERR_PERMISSION, 0);
    EXPECT_LT(Z_ERR_IO, 0);
    EXPECT_LT(Z_ERR_ENCODING, 0);
    EXPECT_LT(Z_ERR_NOMEM, 0);
    EXPECT_LT(Z_ERR_OVERFLOW, 0);
    EXPECT_LT(Z_ERR_UNSUPPORTED, 0);
    EXPECT_LT(Z_ERR_UNKNOWN, 0);
}

TEST(Error, SizeofIsInt) {
    EXPECT_EQ(sizeof(z_error_t), sizeof(int));
}

TEST(Error, MsgNotNull) {
    EXPECT_NE(z_error_msg(Z_OK), nullptr);
    EXPECT_NE(z_error_msg(Z_ERR_INVALID_ARG), nullptr);
    EXPECT_NE(z_error_msg(Z_ERR_UNKNOWN), nullptr);
}

TEST(Error, MsgIsChinese) {
    EXPECT_STREQ(z_error_msg(Z_OK), "成功");
    EXPECT_STREQ(z_error_msg(Z_ERR_INVALID_ARG), "参数非法");
}

TEST(Error, MsgUnknownForOutOfRange) {
    EXPECT_NE(z_error_msg(12345), nullptr);
    EXPECT_STRNE(z_error_msg(12345), "");
}
```

- [ ] **Step 2.4: 跑测试确认编译失败**

```powershell
cmake --build build
```

Expected: 链接错误，找不到 `z_error_msg`。

- [ ] **Step 2.5: 写 error.cpp**

Create `c:\Repos\ZBase\source\src\core\error\error.cpp`:

```cpp
/**
 * @file error.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 错误码中文描述表
 */

#include "zbase/error.h"

namespace {

/// 错误码到描述的映射表
struct ErrorEntry {
    int code;           ///< 错误码
    const char* msg;    ///< 中文描述
};

const ErrorEntry kErrorTable[] = {
    {Z_OK,               "成功"},
    {Z_ERR_INVALID_ARG,  "参数非法"},
    {Z_ERR_NOT_FOUND,    "找不到"},
    {Z_ERR_PERMISSION,   "权限不足"},
    {Z_ERR_IO,           "IO 错误"},
    {Z_ERR_ENCODING,    "编码错误"},
    {Z_ERR_NOMEM,       "内存不足"},
    {Z_ERR_OVERFLOW,    "溢出"},
    {Z_ERR_UNSUPPORTED, "不支持"},
    {Z_ERR_UNKNOWN,     "未知错误"},
};

constexpr size_t kErrorTableSize =
    sizeof(kErrorTable) / sizeof(kErrorTable[0]);

}  // namespace

extern "C" {

const char* z_error_msg(int err) {
    for (size_t i = 0; i < kErrorTableSize; ++i) {
        if (kErrorTable[i].code == err) {
            return kErrorTable[i].msg;
        }
    }
    return "未知错误码";
}

}  // extern "C"
```

- [ ] **Step 2.6: 写 z_version 实现**

由于 `z_version` 在 `zbase.h` 中声明，需要一个 TU 实现。Create `c:\Repos\ZBase\source\src\core\error\zbase_version.cpp`:

```cpp
/**
 * @file zbase_version.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief z_version 函数实现
 */

#include "zbase/zbase.h"

extern "C" {

const char* z_version(void) {
    return ZBASE_VERSION_STRING;
}

}  // extern "C"
```

- [ ] **Step 2.7: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R Error --output-on-failure
```

Expected: 6 个 Error 测试全 PASS。

- [ ] **Step 2.8: Commit**

```powershell
git add source/include/zbase/zbase.h source/include/zbase/error.h source/src/core/error/ source/tests/core/test_error.cpp
git commit -m "feat(error): 实现错误码体系与中文描述"
```

**验收标准：**
- `ctest -R Error` 6 个用例全绿
- `static_assert` 编译期通过

---

## Task 3: string 模块（C++ 内核 + C ABI 待 Task 9 接入）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\string.h`
- Create: `c:\Repos\ZBase\source\src\core\string\string.cpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_string.cpp`

**Goal:** 提供 split / join / replace / trim / tolower / toupper / utf8_len + free / free_array 共 9 个 C ABI 函数。本 Task 实现 C ABI 函数体（直接调 C++ std::string），不分离 core/c_api 两层（YAGNI，参考 phase-1-working-file.md §4.28）。

- [ ] **Step 3.1: 写 string.h**

Create `c:\Repos\ZBase\source\include\zbase\string.h`:

```c
/**
 * @file string.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 字符串工具 C ABI
 */

#ifndef ZBASE_STRING_H_
#define ZBASE_STRING_H_

#include <stddef.h>
#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief 分隔字符串
 * @param input 输入字符串（UTF-8，不以 \0 终止也可，配合 input_len）
 * @param input_len 输入长度（字节）
 * @param sep 分隔符
 * @param out_arr 输出数组（库申请，调 z_string_free_array 释放）
 * @param out_count 输出元素数量
 * @return Z_OK 成功；Z_ERR_INVALID_ARG 参数非法；Z_ERR_NOMEM 内存不足
 */
ZBASE_API int z_string_split(const char* input, size_t input_len,
                             const char* sep,
                             char*** out_arr, size_t* out_count);

/**
 * @brief 拼接字符串数组
 * @param arr 字符串数组
 * @param count 元素数量
 * @param sep 分隔符
 * @param out 输出字符串（库申请，调 z_string_free 释放）
 * @return Z_OK 成功；Z_ERR_INVALID_ARG；Z_ERR_NOMEM
 */
ZBASE_API int z_string_join(char** arr, size_t count, const char* sep, char** out);

/**
 * @brief 替换字符串中的子串
 * @param input 输入
 * @param from 待替换子串
 * @param to 替换为
 * @param out 输出（库申请）
 * @return Z_OK 成功；Z_ERR_INVALID_ARG；Z_ERR_NOMEM
 */
ZBASE_API int z_string_replace(const char* input, const char* from,
                                const char* to, char** out);

/**
 * @brief 去除首尾空白字符
 * @param input 输入
 * @param out 输出（库申请）
 * @return Z_OK 成功；Z_ERR_INVALID_ARG；Z_ERR_NOMEM
 */
ZBASE_API int z_string_trim(const char* input, char** out);

/**
 * @brief 转小写
 * @param input 输入
 * @param out 输出（库申请）
 * @return Z_OK 成功
 */
ZBASE_API int z_string_tolower(const char* input, char** out);

/**
 * @brief 转大写
 */
ZBASE_API int z_string_toupper(const char* input, char** out);

/**
 * @brief 计算 UTF-8 字符串的字符数（不是字节数）
 * @param input 输入
 * @return 字符数；input 为 NULL 时返回 0
 */
ZBASE_API size_t z_string_utf8_len(const char* input);

/**
 * @brief 释放库内申请的字符串
 * @param s 由 z_string_* 申请的字符串指针，可为 NULL
 */
ZBASE_API void z_string_free(char* s);

/**
 * @brief 释放库内申请的字符串数组
 * @param arr 数组指针
 * @param count 元素数量
 */
ZBASE_API void z_string_free_array(char** arr, size_t count);

#ifdef __cplusplus
}
#endif

#endif  // ZBASE_STRING_H_
```

- [ ] **Step 3.2: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_string.cpp`:

```cpp
/**
 * @file test_string.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief string 模块单元测试
 */

#include <gtest/gtest.h>
#include "zbase/string.h"

#include <cstring>

TEST(String, Split_Basic) {
    char** arr = nullptr;
    size_t count = 0;
    const char* input = "a,b,c";
    EXPECT_EQ(z_string_split(input, std::strlen(input), ",", &arr, &count), Z_OK);
    EXPECT_EQ(count, 3u);
    EXPECT_STREQ(arr[0], "a");
    EXPECT_STREQ(arr[1], "b");
    EXPECT_STREQ(arr[2], "c");
    z_string_free_array(arr, count);
}

TEST(String, Split_EmptyField) {
    char** arr = nullptr;
    size_t count = 0;
    const char* input = "a,,c";
    EXPECT_EQ(z_string_split(input, std::strlen(input), ",", &arr, &count), Z_OK);
    EXPECT_EQ(count, 3u);
    EXPECT_STREQ(arr[1], "");
    z_string_free_array(arr, count);
}

TEST(String, Join_Basic) {
    const char* parts[] = {"a", "b", "c"};
    char* out = nullptr;
    EXPECT_EQ(z_string_join(const_cast<char**>(parts), 3, "-", &out), Z_OK);
    EXPECT_STREQ(out, "a-b-c");
    z_string_free(out);
}

TEST(String, Replace_All) {
    char* out = nullptr;
    EXPECT_EQ(z_string_replace("hello world hello", "hello", "hi", &out), Z_OK);
    EXPECT_STREQ(out, "hi world hi");
    z_string_free(out);
}

TEST(String, Trim_BothSides) {
    char* out = nullptr;
    EXPECT_EQ(z_string_trim("  hello  ", &out), Z_OK);
    EXPECT_STREQ(out, "hello");
    z_string_free(out);
}

TEST(String, Trim_NoSpace) {
    char* out = nullptr;
    EXPECT_EQ(z_string_trim("hello", &out), Z_OK);
    EXPECT_STREQ(out, "hello");
    z_string_free(out);
}

TEST(String, Tolower) {
    char* out = nullptr;
    EXPECT_EQ(z_string_tolower("HeLLo", &out), Z_OK);
    EXPECT_STREQ(out, "hello");
    z_string_free(out);
}

TEST(String, Toupper) {
    char* out = nullptr;
    EXPECT_EQ(z_string_toupper("HeLLo", &out), Z_OK);
    EXPECT_STREQ(out, "HELLO");
    z_string_free(out);
}

TEST(String, Utf8Len_Ascii) {
    EXPECT_EQ(z_string_utf8_len("hello"), 5u);
}

TEST(String, Utf8Len_Chinese) {
    // 每个汉字 UTF-8 编码占 3 字节，"你好" 字节数=6，字符数=2
    EXPECT_EQ(z_string_utf8_len("你好"), 2u);
}

TEST(String, Free_NullptrSafe) {
    z_string_free(nullptr);
    z_string_free_array(nullptr, 0);
}

TEST(String, InvalidArg_NullInput) {
    char** arr = nullptr;
    size_t count = 0;
    EXPECT_EQ(z_string_split(nullptr, 0, ",", &arr, &count), Z_ERR_INVALID_ARG);
    EXPECT_EQ(z_string_split("a", 1, nullptr, &arr, &count), Z_ERR_INVALID_ARG);
}
```

- [ ] **Step 3.3: 写实现 string.cpp**

Create `c:\Repos\ZBase\source\src\core\string\string.cpp`:

```cpp
/**
 * @file string.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 字符串工具实现
 */

#include "zbase/string.h"
#include "zbase/error.h"

#include <cstring>
#include <cstdlib>
#include <string>
#include <vector>

namespace {

/// 在堆上分配并复制字符串，返回 char*（调方需用 z_string_free 释放）
char* DupString(const std::string& s) {
    char* p = static_cast<char*>(std::malloc(s.size() + 1));
    if (p == nullptr) return nullptr;
    std::memcpy(p, s.data(), s.size());
    p[s.size()] = '\0';
    return p;
}

}  // namespace

extern "C" {

int z_string_split(const char* input, size_t input_len,
                   const char* sep,
                   char*** out_arr, size_t* out_count) {
    if (input == nullptr || sep == nullptr || out_arr == nullptr || out_count == nullptr) {
        return Z_ERR_INVALID_ARG;
    }
    std::string s(input, input_len);
    std::string sep_str(sep);
    std::vector<std::string> parts;
    if (sep_str.empty()) {
        return Z_ERR_INVALID_ARG;
    }
    size_t start = 0;
    while (true) {
        size_t pos = s.find(sep_str, start);
        if (pos == std::string::npos) {
            parts.emplace_back(s.substr(start));
            break;
        }
        parts.emplace_back(s.substr(start, pos - start));
        start = pos + sep_str.size();
    }
    char** arr = static_cast<char**>(std::calloc(parts.size(), sizeof(char*)));
    if (arr == nullptr) return Z_ERR_NOMEM;
    for (size_t i = 0; i < parts.size(); ++i) {
        arr[i] = DupString(parts[i]);
        if (arr[i] == nullptr) {
            z_string_free_array(arr, i);
            return Z_ERR_NOMEM;
        }
    }
    *out_arr = arr;
    *out_count = parts.size();
    return Z_OK;
}

int z_string_join(char** arr, size_t count, const char* sep, char** out) {
    if (arr == nullptr || sep == nullptr || out == nullptr) {
        return Z_ERR_INVALID_ARG;
    }
    std::string result;
    std::string sep_str(sep);
    for (size_t i = 0; i < count; ++i) {
        if (i > 0) result += sep_str;
        if (arr[i] != nullptr) result += arr[i];
    }
    char* p = DupString(result);
    if (p == nullptr) return Z_ERR_NOMEM;
    *out = p;
    return Z_OK;
}

int z_string_replace(const char* input, const char* from,
                     const char* to, char** out) {
    if (input == nullptr || from == nullptr || to == nullptr || out == nullptr) {
        return Z_ERR_INVALID_ARG;
    }
    std::string s(input);
    std::string from_str(from);
    std::string to_str(to);
    if (from_str.empty()) return Z_ERR_INVALID_ARG;
    size_t pos = 0;
    while ((pos = s.find(from_str, pos)) != std::string::npos) {
        s.replace(pos, from_str.size(), to_str);
        pos += to_str.size();
    }
    char* p = DupString(s);
    if (p == nullptr) return Z_ERR_NOMEM;
    *out = p;
    return Z_OK;
}

int z_string_trim(const char* input, char** out) {
    if (input == nullptr || out == nullptr) return Z_ERR_INVALID_ARG;
    std::string s(input);
    auto is_space = [](unsigned char c) { return std::isspace(c) != 0; };
    size_t b = 0, e = s.size();
    while (b < e && is_space(static_cast<unsigned char>(s[b]))) ++b;
    while (e > b && is_space(static_cast<unsigned char>(s[e - 1]))) --e;
    std::string trimmed = s.substr(b, e - b);
    char* p = DupString(trimmed);
    if (p == nullptr) return Z_ERR_NOMEM;
    *out = p;
    return Z_OK;
}

int z_string_tolower(const char* input, char** out) {
    if (input == nullptr || out == nullptr) return Z_ERR_INVALID_ARG;
    std::string s(input);
    for (char& c : s) c = static_cast<char>(std::tolower(static_cast<unsigned char>(c)));
    char* p = DupString(s);
    if (p == nullptr) return Z_ERR_NOMEM;
    *out = p;
    return Z_OK;
}

int z_string_toupper(const char* input, char** out) {
    if (input == nullptr || out == nullptr) return Z_ERR_INVALID_ARG;
    std::string s(input);
    for (char& c : s) c = static_cast<char>(std::toupper(static_cast<unsigned char>(c)));
    char* p = DupString(s);
    if (p == nullptr) return Z_ERR_NOMEM;
    *out = p;
    return Z_OK;
}

size_t z_string_utf8_len(const char* input) {
    if (input == nullptr) return 0;
    size_t count = 0;
    for (size_t i = 0; input[i] != '\0'; ) {
        unsigned char c = static_cast<unsigned char>(input[i]);
        if (c < 0x80) i += 1;
        else if ((c & 0xE0) == 0xC0) i += 2;
        else if ((c & 0xF0) == 0xE0) i += 3;
        else if ((c & 0xF8) == 0xF0) i += 4;
        else { ++i; }  // 非法字节，按 1 字符算（v0.1 宽容处理）
        ++count;
    }
    return count;
}

void z_string_free(char* s) {
    if (s == nullptr) return;
    std::free(s);
}

void z_string_free_array(char** arr, size_t count) {
    if (arr == nullptr) return;
    for (size_t i = 0; i < count; ++i) {
        z_string_free(arr[i]);
    }
    std::free(arr);
}

}  // extern "C"
```

- [ ] **Step 3.4: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R String --output-on-failure
```

Expected: 12 个 String 测试全 PASS。

- [ ] **Step 3.5: Commit**

```powershell
git add source/include/zbase/string.h source/src/core/string/string.cpp source/tests/core/test_string.cpp
git commit -m "feat(string): 实现 split/join/replace/trim/utf8_len 等 9 个 C ABI"
```

**验收标准：**
- `ctest -R String` 12 个用例全绿
- `dumpbin /exports build\zbase.dll | findstr z_string_` 能看到 9 个导出符号

---

## Task 4: 平台层 file/time 原语

**Files:**
- Create: `c:\Repos\ZBase\source\src\platform\file_platform.hpp`
- Create: `c:\Repos\ZBase\source\src\platform\file_platform.cpp`
- Create: `c:\Repos\ZBase\source\src\platform\time_platform.hpp`
- Create: `c:\Repos\ZBase\source\src\platform\time_platform.cpp`
- Create: `c:\Repos\ZBase\source\tests\platform\test_file_platform.cpp`
- Create: `c:\Repos\ZBase\source\tests\platform\test_time_platform.cpp`

**Goal:** 提供内部文件原语 (UTF-8 路径 → UTF-16 → Win32 `CreateFileW`/`GetFileAttributesW`/`FindFirstFileW` + `\\?\` 长路径前缀) 和时间原语 (Windows `QueryPerformanceCounter` / POSIX `clock_gettime(CLOCK_MONOTONIC)`)。

- [ ] **Step 4.1: 写 file_platform.hpp**

Create `c:\Repos\ZBase\source\src\platform\file_platform.hpp`:

```cpp
/**
 * @file file_platform.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件操作平台原语（内部，不对外导出）
 */

#pragma once

#include <cstdint>
#include <string>

#include "zbase/error.h"

namespace zbase {
namespace platform {

/// 文件属性结构
struct FileStat {
    uint64_t size;      ///< 字节数
    uint64_t mtime;     ///< 修改时间（毫秒 epoch）
    bool     is_dir;    ///< 是否目录
    bool     exists;    ///< 是否存在
};

/**
 * @brief 将 UTF-8 路径转为 Windows 可用的 UTF-16 路径，自动处理 \\?\ 长路径前缀
 * @param utf8_path 输入 UTF-8 路径
 * @param out 输出 std::wstring（Windows）或 std::string（POSIX）
 * @return Z_OK 成功；Z_ERR_ENCODING 编码错误
 */
int NormalizePath(const std::string& utf8_path, std::wstring& out);

/**
 * @brief 同上的 POSIX 重载（直接拷贝）
 */
int NormalizePath(const std::string& utf8_path, std::string& out);

/**
 * @brief 查询文件/目录信息
 */
int FileStatGet(const std::string& utf8_path, FileStat& out);

/**
 * @brief 创建目录（递归）
 */
int MakeDirs(const std::string& utf8_path);

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 4.2: 写 file_platform.cpp**

Create `c:\Repos\ZBase\source\src\platform\file_platform.cpp`:

```cpp
/**
 * @file file_platform.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件操作平台原语实现
 */

#include "platform/file_platform.hpp"
#include "platform/encoding.hpp"

#ifdef _WIN32
#include <Windows.h>
#else
#include <sys/stat.h>
#include <sys/types.h>
#include <dirent.h>
#include <errno.h>
#include <unistd.h>
#include <string.h>
#endif

namespace zbase {
namespace platform {

#ifdef _WIN32

int NormalizePath(const std::string& utf8_path, std::wstring& out) {
    int err = Utf8ToWString(utf8_path, out);
    if (err != Z_OK) return err;
    // 路径长度 > 248 自动加 \\?\ 前缀（详见 phase-1-working-file.md §4.23）
    if (out.size() > 248 && out.substr(0, 4) != L"\\\\?\\") {
        // \\?\ 前缀要求路径用反斜杠
        for (auto& c : out) if (c == L'/') c = L'\\';
        out = L"\\\\?\\" + out;
    }
    return Z_OK;
}

int NormalizePath(const std::string& utf8_path, std::string& out) {
    out = utf8_path;
    return Z_OK;
}

int FileStatGet(const std::string& utf8_path, FileStat& out) {
    std::wstring wpath;
    int err = NormalizePath(utf8_path, wpath);
    if (err != Z_OK) return err;
    WIN32_FILE_ATTRIBUTE_DATA info{};
    if (!GetFileAttributesExW(wpath.c_str(), GetFileExInfoStandard, &info)) {
        DWORD e = GetLastError();
        if (e == ERROR_FILE_NOT_FOUND || e == ERROR_PATH_NOT_FOUND) {
            out.exists = false;
            return Z_OK;
        }
        return (e == ERROR_ACCESS_DENIED) ? Z_ERR_PERMISSION : Z_ERR_IO;
    }
    out.exists = true;
    out.is_dir = (info.dwFileAttributes & FILE_ATTRIBUTE_DIRECTORY) != 0;
    out.size = (static_cast<uint64_t>(info.nFileSizeHigh) << 32) | info.nFileSizeLow;
    // FILETIME 是 100ns 间隔，转毫秒
    uint64_t ft = (static_cast<uint64_t>(info.ftLastWriteTime.dwHighDateTime) << 32)
                  | info.ftLastWriteTime.dwLowDateTime;
    out.mtime = ft / 10000ULL - 11644473600000ULL;  // 1601-01-01 → 1970-01-01
    return Z_OK;
}

int MakeDirs(const std::string& utf8_path) {
    std::wstring wpath;
    int err = NormalizePath(utf8_path, wpath);
    if (err != Z_OK) return err;
    // 逐级创建
    for (size_t i = 1; i < wpath.size(); ++i) {
        if (wpath[i] == L'\\' || wpath[i] == L'/') {
            std::wstring sub = wpath.substr(0, i);
            DWORD attrs = GetFileAttributesW(sub.c_str());
            if (attrs == INVALID_FILE_ATTRIBUTES) {
                if (!CreateDirectoryW(sub.c_str(), nullptr)) {
                    DWORD e = GetLastError();
                    if (e != ERROR_ALREADY_EXISTS) {
                        return (e == ERROR_ACCESS_DENIED) ? Z_ERR_PERMISSION : Z_ERR_IO;
                    }
                }
            }
        }
    }
    DWORD attrs = GetFileAttributesW(wpath.c_str());
    if (attrs == INVALID_FILE_ATTRIBUTES) {
        if (!CreateDirectoryW(wpath.c_str(), nullptr)) {
            DWORD e = GetLastError();
            return (e == ERROR_ALREADY_EXISTS) ? Z_OK
                 : (e == ERROR_ACCESS_DENIED) ? Z_ERR_PERMISSION : Z_ERR_IO;
        }
    }
    return Z_OK;
}

#else  // POSIX

int NormalizePath(const std::string& utf8_path, std::string& out) {
    out = utf8_path;
    return Z_OK;
}

int NormalizePath(const std::string& utf8_path, std::wstring& out) {
    std::wstring tmp;
    int err = Utf8ToWString(utf8_path, tmp);
    if (err != Z_OK) return err;
    out = std::move(tmp);
    return Z_OK;
}

int FileStatGet(const std::string& utf8_path, FileStat& out) {
    struct stat st;
    if (::stat(utf8_path.c_str(), &st) != 0) {
        if (errno == ENOENT) { out.exists = false; return Z_OK; }
        if (errno == EACCES) return Z_ERR_PERMISSION;
        return Z_ERR_IO;
    }
    out.exists = true;
    out.is_dir = S_ISDIR(st.st_mode);
    out.size = static_cast<uint64_t>(st.st_size);
    out.mtime = static_cast<uint64_t>(st.st_mtime) * 1000ULL;
    return Z_OK;
}

int MakeDirs(const std::string& utf8_path) {
    std::string s = utf8_path;
    for (size_t i = 1; i < s.size(); ++i) {
        if (s[i] == '/') {
            std::string sub = s.substr(0, i);
            if (::mkdir(sub.c_str(), 0755) != 0 && errno != EEXIST) {
                return (errno == EACCES) ? Z_ERR_PERMISSION : Z_ERR_IO;
            }
        }
    }
    if (::mkdir(s.c_str(), 0755) != 0 && errno != EEXIST) {
        return (errno == EACCES) ? Z_ERR_PERMISSION : Z_ERR_IO;
    }
    return Z_OK;
}

#endif

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 4.3: 写 time_platform.hpp**

Create `c:\Repos\ZBase\source\src\platform\time_platform.hpp`:

```cpp
/**
 * @file time_platform.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间平台原语（内部）
 */

#pragma once

#include <cstdint>

namespace zbase {
namespace platform {

/**
 * @brief 获取单调时钟纳秒值（用于 perf 高精度计时）
 */
uint64_t MonoNowNs();

/**
 * @brief 获取墙钟毫秒时间戳（epoch 1970）
 */
int64_t WallNowMs();

/**
 * @brief 休眠指定毫秒
 */
void SleepMs(uint32_t ms);

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 4.4: 写 time_platform.cpp**

Create `c:\Repos\ZBase\source\src\platform\time_platform.cpp`:

```cpp
/**
 * @file time_platform.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间原语实现
 */

#include "platform/time_platform.hpp"

#ifdef _WIN32
#include <Windows.h>
#else
#include <time.h>
#include <unistd.h>
#include <sys/time.h>
#endif

namespace zbase {
namespace platform {

#ifdef _WIN32

namespace {
/// QueryPerformanceFrequency 缓存（避免每次调用）
int64_t g_qpf_freq = 0;
}

uint64_t MonoNowNs() {
    if (g_qpf_freq == 0) {
        LARGE_INTEGER freq;
        QueryPerformanceFrequency(&freq);
        g_qpf_freq = freq.QuadPart;
    }
    LARGE_INTEGER counter;
    QueryPerformanceCounter(&counter);
    // counter / freq * 1e9 → counter * 1e9 / freq
    return static_cast<uint64_t>(
        (counter.QuadPart * 1000000000LL) / g_qpf_freq);
}

int64_t WallNowMs() {
    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    uint64_t v = (static_cast<uint64_t>(ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
    return static_cast<int64_t>(v / 10000ULL - 11644473600000ULL);
}

void SleepMs(uint32_t ms) {
    ::Sleep(ms);
}

#else

uint64_t MonoNowNs() {
    struct timespec ts;
    clock_gettime(CLOCK_MONOTONIC, &ts);
    return static_cast<uint64_t>(ts.tv_sec) * 1000000000ULL + ts.tv_nsec;
}

int64_t WallNowMs() {
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return static_cast<int64_t>(ts.tv_sec) * 1000LL + ts.tv_nsec / 1000000LL;
}

void SleepMs(uint32_t ms) {
    struct timespec ts;
    ts.tv_sec = ms / 1000;
    ts.tv_nsec = (ms % 1000) * 1000000L;
    nanosleep(&ts, nullptr);
}

#endif

}  // namespace platform
}  // namespace zbase
```

- [ ] **Step 4.5: 写测试**

Create `c:\Repos\ZBase\source\tests\platform\test_file_platform.cpp`:

```cpp
/**
 * @file test_file_platform.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件平台原语测试
 */

#include <gtest/gtest.h>
#include "platform/file_platform.hpp"

#include <cstdio>
#include <string>

TEST(FilePlatform, Stat_NonExistent) {
    zbase::platform::FileStat st;
    EXPECT_EQ(zbase::platform::FileStatGet("nonexistent_file_zbase_test", st), Z_OK);
    EXPECT_FALSE(st.exists);
}

TEST(FilePlatform, Stat_Existing) {
    std::string path = "zbase_test_file.txt";
    FILE* fp = std::fopen(path.c_str(), "wb");
    ASSERT_NE(fp, nullptr);
    std::fwrite("hello", 1, 5, fp);
    std::fclose(fp);
    zbase::platform::FileStat st;
    EXPECT_EQ(zbase::platform::FileStatGet(path, st), Z_OK);
    EXPECT_TRUE(st.exists);
    EXPECT_FALSE(st.is_dir);
    EXPECT_EQ(st.size, 5u);
    std::remove(path.c_str());
}

TEST(FilePlatform, MakeDirs_Basic) {
    std::string path = "zbase_test_dir/sub";
    zbase::platform::FileStat st;
    EXPECT_EQ(zbase::platform::MakeDirs(path), Z_OK);
    EXPECT_EQ(zbase::platform::FileStatGet(path, st), Z_OK);
    EXPECT_TRUE(st.exists);
    EXPECT_TRUE(st.is_dir);
    std::remove("zbase_test_dir/sub");
    std::remove("zbase_test_dir");
}
```

Create `c:\Repos\ZBase\source\tests\platform\test_time_platform.cpp`:

```cpp
/**
 * @file test_time_platform.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间平台原语测试
 */

#include <gtest/gtest.h>
#include "platform/time_platform.hpp"

TEST(TimePlatform, MonoNow_Increases) {
    uint64_t t1 = zbase::platform::MonoNowNs();
    uint64_t t2 = zbase::platform::MonoNowNs();
    EXPECT_GE(t2, t1);
}

TEST(TimePlatform, WallNow_RecentEpoch) {
    int64_t ms = zbase::platform::WallNowMs();
    // 应该是 2026 年附近（毫秒）
    EXPECT_GT(ms, 1700000000000LL);  // 2023-11 之后
    EXPECT_LT(ms, 2000000000000LL);  // 2033-05 之前
}

TEST(TimePlatform, SleepMs_ActuallySleeps) {
    uint64_t before = zbase::platform::MonoNowNs();
    zbase::platform::SleepMs(50);
    uint64_t after = zbase::platform::MonoNowNs();
    EXPECT_GE(after - before, 40ULL * 1000000ULL);  // 至少 40ms
}
```

- [ ] **Step 4.6: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R "FilePlatform|TimePlatform" --output-on-failure
```

Expected: 6 个测试全 PASS。

- [ ] **Step 4.7: Commit**

```powershell
git add source/src/platform/file_platform.* source/src/platform/time_platform.* source/tests/platform/test_file_platform.cpp source/tests/platform/test_time_platform.cpp
git commit -m "feat(platform): 实现文件/时间平台原语"
```

**验收标准：**
- 6 个平台原语测试全绿
- Windows 上中文路径文件能正确 stat（额外手测，可用 `zbase_test_dir/中文` 路径）

---

## Task 5: time 模块（C ABI）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\time.h`
- Create: `c:\Repos\ZBase\source\src\core\time\time.cpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_time.cpp`

**Goal:** 提供 `z_time_now_ms` / `z_time_now_us` / `z_time_format_iso` / `z_time_sleep_ms` / `z_time_sleep_us` 共 5 个 C ABI 函数。

- [ ] **Step 5.1: 写 time.h**

Create `c:\Repos\ZBase\source\include\zbase\time.h`:

```c
/**
 * @file time.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间工具 C ABI
 */

#ifndef ZBASE_TIME_H_
#define ZBASE_TIME_H_

#include <stddef.h>
#include <stdint.h>
#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief 墙钟毫秒时间戳（epoch 1970）
 */
ZBASE_API int64_t z_time_now_ms(void);

/**
 * @brief 墙钟微秒时间戳
 */
ZBASE_API int64_t z_time_now_us(void);

/**
 * @brief 格式化时间为 ISO 8601 字符串
 * @param ms 毫秒时间戳
 * @param buf 输出缓冲区
 * @param buf_size 缓冲区大小（建议 >= 32）
 * @return Z_OK 成功；Z_ERR_INVALID_ARG buf 为 NULL 或大小不够；Z_ERR_OVERFLOW 缓冲区不足
 * @note 输出形如 "2026-06-29T12:34:56.789"
 */
ZBASE_API int z_time_format_iso(int64_t ms, char* buf, size_t buf_size);

/**
 * @brief 休眠指定毫秒
 */
ZBASE_API void z_time_sleep_ms(uint32_t ms);

/**
 * @brief 休眠指定微秒
 */
ZBASE_API void z_time_sleep_us(uint64_t us);

#ifdef __cplusplus
}
#endif

#endif  // ZBASE_TIME_H_
```

- [ ] **Step 5.2: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_time.cpp`:

```cpp
/**
 * @file test_time.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief time 模块测试
 */

#include <gtest/gtest.h>
#include "zbase/time.h"

#include <cstring>

TEST(Time, NowMs_Recent) {
    int64_t ms = z_time_now_ms();
    EXPECT_GT(ms, 1700000000000LL);
    EXPECT_LT(ms, 2000000000000LL);
}

TEST(Time, NowUs_GreaterThanMs) {
    int64_t us = z_time_now_us();
    int64_t ms = z_time_now_ms();
    EXPECT_GE(us / 1000, ms - 1000);  // 容差 1s
}

TEST(Time, FormatIso_Basic) {
    char buf[32] = {0};
    // 2026-06-29T12:34:56.789 对应毫秒时间戳
    int64_t ms = 1772265296789LL;
    EXPECT_EQ(z_time_format_iso(ms, buf, sizeof(buf)), Z_OK);
    EXPECT_STREQ(buf, "2026-06-29T12:34:56.789");
}

TEST(Time, FormatIso_BufferTooSmall) {
    char buf[10] = {0};
    EXPECT_NE(z_time_format_iso(z_time_now_ms(), buf, sizeof(buf)), Z_OK);
}

TEST(Time, FormatIso_NullBuf) {
    EXPECT_EQ(z_time_format_iso(0, nullptr, 0), Z_ERR_INVALID_ARG);
}

TEST(Time, SleepMs_Blocks) {
    int64_t before = z_time_now_ms();
    z_time_sleep_ms(50);
    int64_t after = z_time_now_ms();
    EXPECT_GE(after - before, 40);
}
```

- [ ] **Step 5.3: 写实现 time.cpp**

Create `c:\Repos\ZBase\source\src\core\time\time.cpp`:

```cpp
/**
 * @file time.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间工具实现
 */

#include "zbase/time.h"
#include "zbase/error.h"
#include "platform/time_platform.hpp"

#ifdef _WIN32
#include <Windows.h>
#else
#include <time.h>
#include <sys/time.h>
#endif

#include <cstdio>
#include <ctime>

extern "C" {

int64_t z_time_now_ms(void) {
    return zbase::platform::WallNowMs();
}

int64_t z_time_now_us(void) {
#ifdef _WIN32
    FILETIME ft;
    GetSystemTimePreciseAsFileTime(&ft);
    uint64_t v = (static_cast<uint64_t>(ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
    // 100ns 间隔 → 微秒
    return static_cast<int64_t>(v / 10ULL - 11644473600000000ULL);
#else
    struct timeval tv;
    gettimeofday(&tv, nullptr);
    return static_cast<int64_t>(tv.tv_sec) * 1000000LL + tv.tv_usec;
#endif
}

int z_time_format_iso(int64_t ms, char* buf, size_t buf_size) {
    if (buf == nullptr || buf_size == 0) return Z_ERR_INVALID_ARG;
    if (buf_size < 24) return Z_ERR_OVERFLOW;
    time_t seconds = static_cast<time_t>(ms / 1000);
    int millis = static_cast<int>(ms % 1000);
    if (millis < 0) { millis += 1000; seconds -= 1; }
    struct tm tmv;
#ifdef _WIN32
    if (localtime_s(&tmv, &seconds) != 0) return Z_ERR_UNKNOWN;
#else
    if (localtime_r(&seconds, &tmv) == nullptr) return Z_ERR_UNKNOWN;
#endif
    int n = std::snprintf(buf, buf_size,
                          "%04d-%02d-%02dT%02d:%02d:%02d.%03d",
                          tmv.tm_year + 1900, tmv.tm_mon + 1, tmv.tm_mday,
                          tmv.tm_hour, tmv.tm_min, tmv.tm_sec, millis);
    if (n < 0 || static_cast<size_t>(n) >= buf_size) return Z_ERR_OVERFLOW;
    return Z_OK;
}

void z_time_sleep_ms(uint32_t ms) {
    zbase::platform::SleepMs(ms);
}

void z_time_sleep_us(uint64_t us) {
#ifdef _WIN32
    // Windows 最小 sleep 精度约 1ms，超过 1ms 用 Sleep，否则忙等
    if (us >= 1000) {
        ::Sleep(static_cast<DWORD>(us / 1000));
    } else {
        uint64_t start = zbase::platform::MonoNowNs();
        while (zbase::platform::MonoNowNs() - start < us * 1000ULL) {
            // 忙等
        }
    }
#else
    struct timespec ts;
    ts.tv_sec = us / 1000000ULL;
    ts.tv_nsec = (us % 1000000ULL) * 1000ULL;
    nanosleep(&ts, nullptr);
#endif
}

}  // extern "C"
```

- [ ] **Step 5.4: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R Time --output-on-failure
```

Expected: 6 个 Time 测试全 PASS。

- [ ] **Step 5.5: Commit**

```powershell
git add source/include/zbase/time.h source/src/core/time/time.cpp source/tests/core/test_time.cpp
git commit -m "feat(time): 实现 now_ms/now_us/format_iso/sleep_ms/sleep_us"
```

**验收标准：** `ctest -R Time` 全绿，ISO 格式输出与本地时区匹配。

---

## Task 6: file 模块（C ABI）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\file.h`
- Create: `c:\Repos\ZBase\source\src\core\file\file_reader.cpp`
- Create: `c:\Repos\ZBase\source\src\core\file\file_writer.cpp`
- Create: `c:\Repos\ZBase\source\src\core\file\dir_iter.cpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_file.cpp`

**Goal:** 提供 `z_file_read_text` / `z_file_write_text` / `z_file_exists` / `z_file_mkdir` / `z_file_free` / `z_dir_iter_create` / `z_dir_iter_next` / `z_dir_iter_destroy` 共 8 个 C ABI 函数。

- [ ] **Step 6.1: 写 file.h（含 POD reserved[8]）**

Create `c:\Repos\ZBase\source\include\zbase\file.h`:

```c
/**
 * @file file.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件操作 C ABI
 */

#ifndef ZBASE_FILE_H_
#define ZBASE_FILE_H_

#include <stddef.h>
#include <stdint.h>
#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief 读取文本文件全部内容
 * @param path UTF-8 路径
 * @param out_buf 输出缓冲区（库内申请，调 z_file_free 释放）
 * @param out_len 输出长度（字节数）
 * @return Z_OK 成功；Z_ERR_NOT_FOUND 文件不存在；Z_ERR_PERMISSION；Z_ERR_IO；Z_ERR_NOMEM
 * @note 二进制模式读取，不转换换行符（详见 phase-1-working-file.md §4.22）
 */
ZBASE_API int z_file_read_text(const char* path, char** out_buf, size_t* out_len);

/**
 * @brief 写文本到文件（原子写：写临时文件 + rename）
 */
ZBASE_API int z_file_write_text(const char* path, const char* content, size_t len);

/**
 * @brief 判断文件/目录是否存在
 * @return 1 存在；0 不存在；负数错误码
 */
ZBASE_API int z_file_exists(const char* path);

/**
 * @brief 递归创建目录
 */
ZBASE_API int z_file_mkdir(const char* path);

/**
 * @brief 释放库内申请的文件缓冲区（nullptr 安全）
 */
ZBASE_API void z_file_free(void* p);

/// 目录遍历条目（POD，详见 architecture.md §13.2）
typedef struct {
    const char* name;   ///< 文件名（UTF-8，库内申请，iter 销毁时释放）
    int          is_dir; ///< 是否目录
    uint64_t     size;   ///< 字节数
    uint64_t     mtime;  ///< 毫秒时间戳
    void* reserved[8];   ///< 预留字段，调用方必须置零
} z_dir_iter_entry_t;

/// 不透明迭代器句柄
typedef struct z_dir_iter z_dir_iter;

/**
 * @brief 创建目录迭代器
 * @param path 目录路径（UTF-8）
 * @param out_iter 输出迭代器
 * @return Z_OK 成功；Z_ERR_NOT_FOUND；Z_ERR_PERMISSION
 */
ZBASE_API int z_dir_iter_create(const char* path, z_dir_iter** out_iter);

/**
 * @brief 获取下一条目
 * @param iter 迭代器
 * @param out_entry 输出条目（迭代器内部所有，next 调用时上一条目失效）
 * @return Z_OK 取到条目；Z_ERR_NOT_FOUND 遍历结束；其他错误码
 */
ZBASE_API int z_dir_iter_next(z_dir_iter* iter, z_dir_iter_entry_t* out_entry);

/**
 * @brief 销毁迭代器（释放内部资源，iter 置 NULL 由调用方负责）
 */
ZBASE_API void z_dir_iter_destroy(z_dir_iter* iter);

#ifdef __cplusplus
}
#endif

#endif  // ZBASE_FILE_H_
```

- [ ] **Step 6.2: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_file.cpp`:

```cpp
/**
 * @file test_file.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief file 模块测试
 */

#include <gtest/gtest.h>
#include "zbase/file.h"

#include <cstdio>
#include <cstring>

TEST(File, ReadText_NotFound) {
    char* buf = nullptr;
    size_t len = 0;
    EXPECT_EQ(z_file_read_text("nonexistent_zbase.txt", &buf, &len), Z_ERR_NOT_FOUND);
    EXPECT_EQ(buf, nullptr);
}

TEST(File, WriteThenRead) {
    const char* path = "zbase_test_writeread.txt";
    const char* content = "hello zbase\nsecond line";
    EXPECT_EQ(z_file_write_text(path, content, std::strlen(content)), Z_OK);
    char* buf = nullptr;
    size_t len = 0;
    EXPECT_EQ(z_file_read_text(path, &buf, &len), Z_OK);
    ASSERT_NE(buf, nullptr);
    EXPECT_EQ(len, std::strlen(content));
    EXPECT_EQ(std::string(buf, len), std::string(content));
    z_file_free(buf);
    std::remove(path);
}

TEST(File, Exists) {
    EXPECT_EQ(z_file_exists("nonexistent_zbase.txt"), 0);
    const char* path = "zbase_test_exists.txt";
    FILE* fp = std::fopen(path.c_str(), "wb");
    ASSERT_NE(fp, nullptr);
    std::fwrite("x", 1, 1, fp);
    std::fclose(fp);
    EXPECT_EQ(z_file_exists(path), 1);
    std::remove(path);
}

TEST(File, Mkdir) {
    const char* path = "zbase_test_mkdir/sub";
    EXPECT_EQ(z_file_mkdir(path), Z_OK);
    EXPECT_EQ(z_file_exists(path), 1);
    std::remove("zbase_test_mkdir/sub");
    std::remove("zbase_test_mkdir");
}

TEST(File, Free_NullptrSafe) {
    z_file_free(nullptr);
}

TEST(File, DirIter_Basic) {
    const char* dir = "zbase_test_diriter";
    ASSERT_EQ(z_file_mkdir(dir), Z_OK);
    // 创建几个文件
    const char* files[] = {"a.txt", "b.txt"};
    for (const char* f : files) {
        std::string full = std::string(dir) + "/" + f;
        EXPECT_EQ(z_file_write_text(full.c_str(), "x", 1), Z_OK);
    }
    z_dir_iter* iter = nullptr;
    EXPECT_EQ(z_dir_iter_create(dir, &iter), Z_OK);
    ASSERT_NE(iter, nullptr);
    int count = 0;
    z_dir_iter_entry_t entry;
    while (z_dir_iter_next(iter, &entry) == Z_OK) {
        if (strcmp(entry.name, ".") == 0 || strcmp(entry.name, "..") == 0) continue;
        ++count;
    }
    EXPECT_GE(count, 2);  // 至少 2 个文件
    z_dir_iter_destroy(iter);
    std::remove("zbase_test_diriter/a.txt");
    std::remove("zbase_test_diriter/b.txt");
    std::remove("zbase_test_diriter");
}

TEST(File, ReadText_InvalidArg) {
    EXPECT_EQ(z_file_read_text(nullptr, nullptr, nullptr), Z_ERR_INVALID_ARG);
}
```

- [ ] **Step 6.3: 写 file_reader.cpp**

Create `c:\Repos\ZBase\source\src\core\file\file_reader.cpp`:

```cpp
/**
 * @file file_reader.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件读取实现
 */

#include "zbase/file.h"
#include "zbase/error.h"
#include "platform/file_platform.hpp"
#include "platform/encoding.hpp"

#include <cstdlib>
#include <cstring>

#ifdef _WIN32
#include <Windows.h>
#else
#include <fcntl.h>
#include <unistd.h>
#include <sys/stat.h>
#endif

extern "C" {

int z_file_read_text(const char* path, char** out_buf, size_t* out_len) {
    if (path == nullptr || out_buf == nullptr || out_len == nullptr) {
        return Z_ERR_INVALID_ARG;
    }
    *out_buf = nullptr;
    *out_len = 0;

    zbase::platform::FileStat st;
    int err = zbase::platform::FileStatGet(path, st);
    if (err != Z_OK) return err;
    if (!st.exists) return Z_ERR_NOT_FOUND;
    if (st.is_dir) return Z_ERR_INVALID_ARG;

#ifdef _WIN32
    std::wstring wpath;
    err = zbase::platform::NormalizePath(path, wpath);
    if (err != Z_OK) return err;
    HANDLE h = CreateFileW(wpath.c_str(), GENERIC_READ, FILE_SHARE_READ, nullptr,
                           OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, nullptr);
    if (h == INVALID_HANDLE_VALUE) {
        DWORD e = GetLastError();
        return (e == ERROR_ACCESS_DENIED) ? Z_ERR_PERMISSION : Z_ERR_IO;
    }
    DWORD size = static_cast<DWORD>(st.size);
    char* buf = static_cast<char*>(std::malloc(size ? size : 1));
    if (buf == nullptr) { CloseHandle(h); return Z_ERR_NOMEM; }
    DWORD read_bytes = 0;
    if (!ReadFile(h, buf, size, &read_bytes, nullptr)) {
        CloseHandle(h);
        std::free(buf);
        return Z_ERR_IO;
    }
    CloseHandle(h);
    *out_buf = buf;
    *out_len = read_bytes;
    return Z_OK;
#else
    int fd = ::open(path, O_RDONLY | O_BINARY);
    if (fd < 0) {
        return (errno == EACCES) ? Z_ERR_PERMISSION : Z_ERR_IO;
    }
    char* buf = static_cast<char*>(std::malloc(st.size ? st.size : 1));
    if (buf == nullptr) { ::close(fd); return Z_ERR_NOMEM; }
    size_t total = 0;
    while (total < st.size) {
        ssize_t n = ::read(fd, buf + total, st.size - total);
        if (n < 0) { ::close(fd); std::free(buf); return Z_ERR_IO; }
        if (n == 0) break;
        total += static_cast<size_t>(n);
    }
    ::close(fd);
    *out_buf = buf;
    *out_len = total;
    return Z_OK;
#endif
}

}  // extern "C"
```

- [ ] **Step 6.4: 写 file_writer.cpp**

Create `c:\Repos\ZBase\source\src\core\file\file_writer.cpp`:

```cpp
/**
 * @file file_writer.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件写入实现（原子写：写临时文件 + rename）
 */

#include "zbase/file.h"
#include "zbase/error.h"
#include "platform/file_platform.hpp"

#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <string>

#ifdef _WIN32
#include <Windows.h>
#else
#include <fcntl.h>
#include <unistd.h>
#endif

extern "C" {

int z_file_write_text(const char* path, const char* content, size_t len) {
    if (path == nullptr || (content == nullptr && len > 0)) {
        return Z_ERR_INVALID_ARG;
    }
    std::string tmp = std::string(path) + ".zbase.tmp";

#ifdef _WIN32
    std::wstring wtmp;
    int err = zbase::platform::NormalizePath(tmp, wtmp);
    if (err != Z_OK) return err;
    HANDLE h = CreateFileW(wtmp.c_str(), GENERIC_WRITE, 0, nullptr,
                           CREATE_ALWAYS, FILE_ATTRIBUTE_NORMAL, nullptr);
    if (h == INVALID_HANDLE_VALUE) {
        DWORD e = GetLastError();
        return (e == ERROR_ACCESS_DENIED) ? Z_ERR_PERMISSION : Z_ERR_IO;
    }
    DWORD written = 0;
    if (len > 0) {
        if (!WriteFile(h, content, static_cast<DWORD>(len), &written, nullptr)) {
            CloseHandle(h);
            return Z_ERR_IO;
        }
    }
    CloseHandle(h);
    if (written != len) return Z_ERR_IO;

    std::wstring wpath;
    err = zbase::platform::NormalizePath(path, wpath);
    if (err != Z_OK) return err;
    if (!MoveFileExW(wtmp.c_str(), wpath.c_str(),
                     MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)) {
        return Z_ERR_IO;
    }
    return Z_OK;
#else
    int fd = ::open(tmp.c_str(), O_WRONLY | O_CREAT | O_TRUNC | O_BINARY, 0644);
    if (fd < 0) return (errno == EACCES) ? Z_ERR_PERMISSION : Z_ERR_IO;
    size_t total = 0;
    while (total < len) {
        ssize_t n = ::write(fd, content + total, len - total);
        if (n < 0) { ::close(fd); ::unlink(tmp.c_str()); return Z_ERR_IO; }
        total += static_cast<size_t>(n);
    }
    ::close(fd);
    if (::rename(tmp.c_str(), path) != 0) {
        ::unlink(tmp.c_str());
        return Z_ERR_IO;
    }
    return Z_OK;
#endif
}

int z_file_exists(const char* path) {
    if (path == nullptr) return Z_ERR_INVALID_ARG;
    zbase::platform::FileStat st;
    int err = zbase::platform::FileStatGet(path, st);
    if (err != Z_OK) return err;
    return st.exists ? 1 : 0;
}

int z_file_mkdir(const char* path) {
    if (path == nullptr) return Z_ERR_INVALID_ARG;
    return zbase::platform::MakeDirs(path);
}

void z_file_free(void* p) {
    if (p == nullptr) return;
    std::free(p);
}

}  // extern "C"
```

- [ ] **Step 6.5: 写 dir_iter.cpp**

Create `c:\Repos\ZBase\source\src\core\file\dir_iter.cpp`:

```cpp
/**
 * @file dir_iter.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 目录遍历实现
 */

#include "zbase/file.h"
#include "zbase/error.h"
#include "platform/file_platform.hpp"
#include "platform/encoding.hpp"

#include <cstring>
#include <string>
#include <vector>

#ifdef _WIN32
#include <Windows.h>
#else
#include <dirent.h>
#include <sys/stat.h>
#endif

struct z_dir_iter {
#ifdef _WIN32
    HANDLE find_handle = INVALID_HANDLE_VALUE;
    WIN32_FIND_DATAW find_data;
    bool first = true;
#else
    DIR* dir = nullptr;
    std::string base_path;
#endif
    std::string current_name;  ///< 当前条目名（UTF-8）
};

extern "C" {

int z_dir_iter_create(const char* path, z_dir_iter** out_iter) {
    if (path == nullptr || out_iter == nullptr) return Z_ERR_INVALID_ARG;
    *out_iter = nullptr;

    zbase::platform::FileStat st;
    int err = zbase::platform::FileStatGet(path, st);
    if (err != Z_OK) return err;
    if (!st.exists) return Z_ERR_NOT_FOUND;
    if (!st.is_dir) return Z_ERR_INVALID_ARG;

    z_dir_iter* it = new (std::nothrow) z_dir_iter();
    if (it == nullptr) return Z_ERR_NOMEM;

#ifdef _WIN32
    std::wstring wpath;
    err = zbase::platform::NormalizePath(std::string(path) + "/*", wpath);
    if (err != Z_OK) { delete it; return err; }
    it->find_handle = FindFirstFileW(wpath.c_str(), &it->find_data);
    if (it->find_handle == INVALID_HANDLE_VALUE) {
        delete it;
        return Z_ERR_IO;
    }
    it->first = true;
#else
    it->dir = ::opendir(path);
    if (it->dir == nullptr) {
        delete it;
        return Z_ERR_IO;
    }
    it->base_path = path;
#endif

    *out_iter = it;
    return Z_OK;
}

int z_dir_iter_next(z_dir_iter* iter, z_dir_iter_entry_t* out_entry) {
    if (iter == nullptr || out_entry == nullptr) return Z_ERR_INVALID_ARG;
    // 校验 reserved 必须置零（详见 architecture.md §13.2）
    for (size_t i = 0; i < 8; ++i) {
        if (out_entry->reserved[i] != nullptr) return Z_ERR_INVALID_ARG;
    }

    std::memset(out_entry, 0, sizeof(*out_entry));

#ifdef _WIN32
    while (true) {
        if (!iter->first) {
            if (!FindNextFileW(iter->find_handle, &iter->find_data)) {
                DWORD e = GetLastError();
                if (e == ERROR_NO_MORE_FILES) return Z_ERR_NOT_FOUND;
                return Z_ERR_IO;
            }
        }
        iter->first = false;

        std::wstring wname(iter->find_data.cFileName);
        std::string name;
        zbase::platform::WStringToUtf8(wname, name);
        iter->current_name = std::move(name);

        out_entry->name = iter->current_name.c_str();
        out_entry->is_dir = (iter->find_data.dwFileAttributes & FILE_ATTRIBUTE_DIRECTORY) != 0 ? 1 : 0;
        out_entry->size = (static_cast<uint64_t>(iter->find_data.nFileSizeHigh) << 32)
                          | iter->find_data.nFileSizeLow;
        uint64_t ft = (static_cast<uint64_t>(iter->find_data.ftLastWriteTime.dwHighDateTime) << 32)
                      | iter->find_data.ftLastWriteTime.dwLowDateTime;
        out_entry->mtime = ft / 10000ULL - 11644473600000ULL;
        return Z_OK;
    }
#else
    while (true) {
        struct dirent* ent = ::readdir(iter->dir);
        if (ent == nullptr) return Z_ERR_NOT_FOUND;
        iter->current_name = ent->d_name;
        std::string full = iter->base_path + "/" + ent->d_name;
        zbase::platform::FileStat st;
        int err = zbase::platform::FileStatGet(full, st);
        if (err != Z_OK || !st.exists) continue;
        out_entry->name = iter->current_name.c_str();
        out_entry->is_dir = st.is_dir ? 1 : 0;
        out_entry->size = st.size;
        out_entry->mtime = st.mtime;
        return Z_OK;
    }
#endif
}

void z_dir_iter_destroy(z_dir_iter* iter) {
    if (iter == nullptr) return;
#ifdef _WIN32
    if (iter->find_handle != INVALID_HANDLE_VALUE) {
        FindClose(iter->find_handle);
    }
#else
    if (iter->dir != nullptr) {
        ::closedir(iter->dir);
    }
#endif
    delete iter;
}

}  // extern "C"
```

- [ ] **Step 6.6: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R File --output-on-failure
```

Expected: 7 个 File 测试全 PASS。

- [ ] **Step 6.7: Commit**

```powershell
git add source/include/zbase/file.h source/src/core/file/ source/tests/core/test_file.cpp
git commit -m "feat(file): 实现文件读写/exists/mkdir/dir_iter 共 8 个 C ABI"
```

**验收标准：**
- 7 个测试全绿
- 手测中文路径文件读写（v0.2 加自动测试）

---

## Task 7: perf 模块（C ABI）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\perf.h`
- Create: `c:\Repos\ZBase\source\src\core\perf\perf.cpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_perf.cpp`

**Goal:** 提供 `z_perf_start` / `z_perf_end` / `z_perf_dump` / `z_perf_reset` + `Z_PERF_SCOPE` 宏。

- [ ] **Step 7.1: 写 perf.h**

Create `c:\Repos\ZBase\source\include\zbase\perf.h`:

```c
/**
 * @file perf.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 性能统计 C ABI
 */

#ifndef ZBASE_PERF_H_
#define ZBASE_PERF_H_

#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief 开始命名打点
 * @param name 打点名（建议 ≤ 64 字节）
 * @return Z_OK 成功；Z_ERR_INVALID_ARG name 为 NULL；Z_ERR_NOMEM
 */
ZBASE_API int z_perf_start(const char* name);

/**
 * @brief 结束命名打点（累计到同名计数器）
 * @return Z_OK 成功；Z_ERR_NOT_FOUND 该 name 从未 start
 */
ZBASE_API int z_perf_end(const char* name);

/**
 * @brief 输出所有打点到 stderr（按总耗时降序）
 */
ZBASE_API void z_perf_dump(void);

/**
 * @brief 清空所有打点
 */
ZBASE_API void z_perf_reset(void);

#ifdef __cplusplus
}
#endif

#ifdef __cplusplus

#include <cstdint>
#include <string_view>

namespace zbase {

/// RAII 性能打点（C++ 包装，header-only）
class PerfScope {
 public:
  explicit PerfScope(const char* name) : name_(name) { z_perf_start(name_); }
  explicit PerfScope(std::string_view name) : name_(name.data()) { z_perf_start(name_); }
  ~PerfScope() { if (name_) z_perf_end(name_); }
  PerfScope(const PerfScope&) = delete;
  PerfScope& operator=(const PerfScope&) = delete;
 private:
  const char* name_;
};

}  // namespace zbase

/// 作用域打点宏（C++ 用）
#define Z_PERF_SCOPE(name) ::zbase::PerfScope _zperf_scope_##__LINE__(name)

#endif  // __cplusplus

#endif  // ZBASE_PERF_H_
```

- [ ] **Step 7.2: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_perf.cpp`:

```cpp
/**
 * @file test_perf.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief perf 模块测试
 */

#include <gtest/gtest.h>
#include "zbase/perf.h"
#include "zbase/time.h"

TEST(Perf, StartEnd_NoCrash) {
    z_perf_reset();
    EXPECT_EQ(z_perf_start("task1"), Z_OK);
    z_time_sleep_ms(10);
    EXPECT_EQ(z_perf_end("task1"), Z_OK);
}

TEST(Perf, End_NotStarted) {
    z_perf_reset();
    EXPECT_EQ(z_perf_end("never_started"), Z_ERR_NOT_FOUND);
}

TEST(Perf, Start_NullName) {
    EXPECT_EQ(z_perf_start(nullptr), Z_ERR_INVALID_ARG);
}

TEST(Perf, Dump_NoCrash) {
    z_perf_reset();
    z_perf_start("dumpable");
    z_perf_end("dumpable");
    z_perf_dump();
    SUCCEED();
}

TEST(Perf, Reset_ClearsAll) {
    z_perf_start("a");
    z_perf_end("a");
    z_perf_reset();
    EXPECT_EQ(z_perf_end("a"), Z_ERR_NOT_FOUND);
}

TEST(Perf, Scope_Macro) {
    z_perf_reset();
    {
        Z_PERF_SCOPE("scoped_task");
        z_time_sleep_ms(5);
    }
    // 作用域结束后应能查到该 name 的累计（通过 dump 不崩验证）
    z_perf_dump();
    SUCCEED();
}
```

- [ ] **Step 7.3: 写实现 perf.cpp**

Create `c:\Repos\ZBase\source\src\core\perf\perf.cpp`:

```cpp
/**
 * @file perf.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 性能统计实现
 */

#include "zbase/perf.h"
#include "zbase/error.h"
#include "platform/time_platform.hpp"

#include <cstdint>
#include <cstdio>
#include <mutex>
#include <string>
#include <unordered_map>

namespace {

struct Accumulator {
    uint64_t total_ns = 0;   ///< 累计纳秒
    uint64_t count = 0;      ///< 调用次数
};

struct PendingStart {
    uint64_t start_ns = 0;
    int      ref_count = 0;  ///< 同线程多次 start 计数（v0.1 不严格区分线程）
};

std::mutex g_mutex;
std::unordered_map<std::string, Accumulator> g_accum;
std::unordered_map<std::string, PendingStart> g_pending;

}  // namespace

extern "C" {

int z_perf_start(const char* name) {
    if (name == nullptr) return Z_ERR_INVALID_ARG;
    std::lock_guard<std::mutex> lock(g_mutex);
    auto& p = g_pending[name];
    if (p.ref_count == 0) {
        p.start_ns = zbase::platform::MonoNowNs();
    }
    ++p.ref_count;
    return Z_OK;
}

int z_perf_end(const char* name) {
    if (name == nullptr) return Z_ERR_INVALID_ARG;
    uint64_t now = zbase::platform::MonoNowNs();
    std::lock_guard<std::mutex> lock(g_mutex);
    auto it = g_pending.find(name);
    if (it == g_pending.end() || it->second.ref_count == 0) {
        return Z_ERR_NOT_FOUND;
    }
    PendingStart& p = it->second;
    uint64_t elapsed = now - p.start_ns;
    --p.ref_count;
    if (p.ref_count == 0) {
        g_pending.erase(it);
    }
    auto& a = g_accum[name];
    a.total_ns += elapsed;
    a.count += 1;
    return Z_OK;
}

void z_perf_dump(void) {
    std::lock_guard<std::mutex> lock(g_mutex);
    // 排序：按 total_ns 降序
    std::vector<std::pair<std::string, Accumulator>> v(g_accum.begin(), g_accum.end());
    std::sort(v.begin(), v.end(),
        [](const auto& a, const auto& b) { return a.second.total_ns > b.second.total_ns; });
    std::fprintf(stderr, "==== zbase perf dump ====\n");
    for (const auto& [name, acc] : v) {
        double ms = acc.total_ns / 1000000.0;
        std::fprintf(stderr, "  %-30s %10.3f ms  %10llu 次  平均 %.3f ms\n",
                      name.c_str(), ms,
                      static_cast<unsigned long long>(acc.count),
                      acc.count ? ms / acc.count : 0.0);
    }
    std::fprintf(stderr, "=========================\n");
}

void z_perf_reset(void) {
    std::lock_guard<std::mutex> lock(g_mutex);
    g_accum.clear();
    g_pending.clear();
}

}  // extern "C"
```

注意：perf.cpp 需 `#include <algorithm>` 和 `#include <vector>`。修正：

```cpp
#include <algorithm>
#include <vector>
```

将这两行加入 perf.cpp 顶部 include 区。

- [ ] **Step 7.4: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R Perf --output-on-failure
```

Expected: 6 个 Perf 测试全 PASS。

- [ ] **Step 7.5: Commit**

```powershell
git add source/include/zbase/perf.h source/src/core/perf/perf.cpp source/tests/core/test_perf.cpp
git commit -m "feat(perf): 实现 start/end/dump/reset + Z_PERF_SCOPE 宏"
```

**验收标准：** 6 个测试全绿，dump 输出中文标签。

---

## Task 8: log 模块（最小版 C ABI）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\log.h`
- Create: `c:\Repos\ZBase\source\src\core\log\log.cpp`
- Create: `c:\Repos\ZBase\source\src\core\log\console.cpp`
- Create: `c:\Repos\ZBase\source\src\core\log\console.hpp`
- Create: `c:\Repos\ZBase\source\tests\core\test_log.cpp`

**Goal:** 提供 `z_log_init` / `z_log_shutdown` + 4 级日志宏。Windows 下 `SetConsoleOutputCP(CP_UTF8)` 并 save/restore；调一次 `SetDefaultDllDirectories`。

- [ ] **Step 8.1: 写 log.h**

Create `c:\Repos\ZBase\source\include\zbase\log.h`:

```c
/**
 * @file log.h
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 日志 C ABI（最小版）
 */

#ifndef ZBASE_LOG_H_
#define ZBASE_LOG_H_

#include "zbase/zbase.h"

#ifdef __cplusplus
extern "C" {
#endif

typedef enum {
    Z_LOG_LEVEL_DEBUG = 0,
    Z_LOG_LEVEL_INFO  = 1,
    Z_LOG_LEVEL_WARN  = 2,
    Z_LOG_LEVEL_ERROR = 3,
} z_log_level_t;

/**
 * @brief 初始化日志系统
 * @param level 最低输出级别
 * @return Z_OK 成功
 * @note Windows 下会：① save 原 console CP ② SetConsoleOutputCP(CP_UTF8)
 *       ③ 调 SetDefaultDllDirectories 限制 DLL 搜索路径
 */
ZBASE_API int z_log_init(z_log_level_t level);

/**
 * @brief 关闭日志，恢复控制台 CP
 */
ZBASE_API void z_log_shutdown(void);

/**
 * @brief 写日志（内部接口，建议用宏）
 */
ZBASE_API void z_log_write(z_log_level_t level, const char* file, int line,
                           const char* fmt, ...);

#ifdef __cplusplus
}
#endif

#define Z_LOG_DEBUG(fmt, ...) z_log_write(Z_LOG_LEVEL_DEBUG, __FILE__, __LINE__, fmt, ##__VA_ARGS__)
#define Z_LOG_INFO(fmt, ...)  z_log_write(Z_LOG_LEVEL_INFO,  __FILE__, __LINE__, fmt, ##__VA_ARGS__)
#define Z_LOG_WARN(fmt, ...)  z_log_write(Z_LOG_LEVEL_WARN,  __FILE__, __LINE__, fmt, ##__VA_ARGS__)
#define Z_LOG_ERROR(fmt, ...) z_log_write(Z_LOG_LEVEL_ERROR, __FILE__, __LINE__, fmt, ##__VA_ARGS__)

#endif  // ZBASE_LOG_H_
```

- [ ] **Step 8.2: 写 console.hpp / console.cpp**

Create `c:\Repos\ZBase\source\src\core\log\console.hpp`:

```cpp
/**
 * @file console.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 控制台 UTF-8 设置（Windows save/restore，详见 phase-1-working-file.md §4.21）
 */

#pragma once

namespace zbase {
namespace log {

/// 设置控制台为 UTF-8（Windows 保存原 CP）
void InitConsoleUtf8();

/// 恢复原控制台 CP
void RestoreConsole();

}  // namespace log
}  // namespace zbase
```

Create `c:\Repos\ZBase\source\src\core\log\console.cpp`:

```cpp
/**
 * @file console.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 控制台 UTF-8 设置实现
 */

#include "console.hpp"

#ifdef _WIN32
#include <Windows.h>

namespace zbase {
namespace log {

namespace {
UINT g_orig_cp = 0;       ///< 原输入 CP
UINT g_orig_output_cp = 0; ///< 原输出 CP
bool g_initialized = false;
}

void InitConsoleUtf8() {
    if (g_initialized) return;
    g_orig_cp = GetConsoleCP();
    g_orig_output_cp = GetConsoleOutputCP();
    SetConsoleCP(CP_UTF8);
    SetConsoleOutputCP(CP_UTF8);
    // 限制 DLL 搜索路径（详见 phase-1-working-file.md §4.26）
    SetDefaultDllDirectories(LOAD_LIBRARY_SEARCH_APPLICATION_DIR
                             | LOAD_LIBRARY_SEARCH_SYSTEM32);
    g_initialized = true;
}

void RestoreConsole() {
    if (!g_initialized) return;
    if (g_orig_cp) SetConsoleCP(g_orig_cp);
    if (g_orig_output_cp) SetConsoleOutputCP(g_orig_output_cp);
    g_initialized = false;
}

}  // namespace log
}  // namespace zbase

#else  // POSIX

namespace zbase {
namespace log {

void InitConsoleUtf8() {}
void RestoreConsole() {}

}  // namespace log
}  // namespace zbase

#endif
```

- [ ] **Step 8.3: 写 log.cpp**

Create `c:\Repos\ZBase\source\src\core\log\log.cpp`:

```cpp
/**
 * @file log.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 日志实现（最小版，stderr 输出）
 */

#include "zbase/log.h"
#include "zbase/time.h"
#include "console.hpp"

#include <atomic>
#include <cstdarg>
#include <cstdio>
#include <cstring>

namespace {

std::atomic<int> g_min_level{Z_LOG_LEVEL_INFO};

const char* LevelName(z_log_level_t l) {
    switch (l) {
        case Z_LOG_LEVEL_DEBUG: return "DEBUG";
        case Z_LOG_LEVEL_INFO:  return "INFO";
        case Z_LOG_LEVEL_WARN:  return "WARN";
        case Z_LOG_LEVEL_ERROR: return "ERROR";
        default:                return "?";
    }
}

}  // namespace

extern "C" {

int z_log_init(z_log_level_t level) {
    zbase::log::InitConsoleUtf8();
    g_min_level.store(static_cast<int>(level), std::memory_order_relaxed);
    return Z_OK;
}

void z_log_shutdown(void) {
    zbase::log::RestoreConsole();
}

void z_log_write(z_log_level_t level, const char* file, int line,
                 const char* fmt, ...) {
    if (static_cast<int>(level) < g_min_level.load(std::memory_order_relaxed)) {
        return;
    }
    char time_buf[32];
    int64_t ms = z_time_now_ms();
    z_time_format_iso(ms, time_buf, sizeof(time_buf));

    // 提取文件 basename
    const char* base = file;
    if (const char* p = std::strrchr(file, '/'))  base = p + 1;
    if (const char* p = std::strrchr(base, '\\')) base = p + 1;

    char user_buf[1024];
    va_list args;
    va_start(args, fmt);
    std::vsnprintf(user_buf, sizeof(user_buf), fmt, args);
    va_end(args);

    std::fprintf(stderr, "[%s] [%s] [%s:%d] %s\n",
                 time_buf, LevelName(level), base, line, user_buf);
    std::fflush(stderr);
}

}  // extern "C"
```

- [ ] **Step 8.4: 写测试**

Create `c:\Repos\ZBase\source\tests\core\test_log.cpp`:

```cpp
/**
 * @file test_log.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief log 模块测试
 */

#include <gtest/gtest.h>
#include "zbase/log.h"

TEST(Log, InitShutdown) {
    EXPECT_EQ(z_log_init(Z_LOG_LEVEL_DEBUG), Z_OK);
    z_log_shutdown();
    SUCCEED();
}

TEST(Log, Write_NoCrash) {
    z_log_init(Z_LOG_LEVEL_DEBUG);
    Z_LOG_INFO("测试中文日志 %d", 42);
    Z_LOG_WARN("warning %s", "msg");
    Z_LOG_ERROR("error message");
    z_log_shutdown();
    SUCCEED();
}

TEST(Log, LevelFilter) {
    z_log_init(Z_LOG_LEVEL_ERROR);
    Z_LOG_DEBUG("should not appear");
    Z_LOG_INFO("should not appear");
    Z_LOG_ERROR("should appear");
    z_log_shutdown();
    SUCCEED();
}
```

- [ ] **Step 8.5: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R Log --output-on-failure
```

Expected: 3 个 Log 测试全 PASS，stderr 上能看到中文日志输出。

- [ ] **Step 8.6: Commit**

```powershell
git add source/include/zbase/log.h source/src/core/log/ source/tests/core/test_log.cpp
git commit -m "feat(log): 实现分级日志 + 控制台 UTF-8 + DLL 搜索路径限制"
```

**验收标准：**
- 3 个测试全绿
- Windows 控制台输出中文不乱码

---

## Task 9: C ABI 导出层封装 + 异常包裹宏

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase\internal\c_api_guard.hpp`
- Create: `c:\Repos\ZBase\source\src\c_api\error_c.cpp`
- Create: `c:\Repos\ZBase\source\src\c_api\string_c.cpp`
- Create: `c:\Repos\ZBase\source\src\c_api\time_c.cpp`
- Create: `c:\Repos\ZBase\source\src\c_api\file_c.cpp`
- Create: `c:\Repos\ZBase\source\src\c_api\perf_c.cpp`
- Create: `c:\Repos\ZBase\source\src\c_api\log_c.cpp`

**Goal:** 把 Task 2-8 的 C ABI 函数（已经在各自模块导出）统一用 `ZBASE_C_API_BEGIN/END` 宏包裹，防止异常逃逸。本 Task 主要是**重构**：把现有 `extern "C"` 函数体包进宏。

> **注：** Task 3-8 已经直接 `extern "C"` 导出了 C ABI。本 Task **不重复实现**，而是引入异常包裹宏，并把各模块的 `*_c.cpp` 作为薄薄的转发层（与 phase-1-working-file.md §3.7 一致）。考虑到 YAGNI 守门（§4.28），v0.1 选择 **直接在模块 cpp 内用宏包裹**，不分离 `c_api/` 目录。

- [ ] **Step 9.1: 写 c_api_guard.hpp**

Create `c:\Repos\ZBase\source\include\zbase\internal\c_api_guard.hpp`:

```cpp
/**
 * @file c_api_guard.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief C ABI 异常包裹宏（内部头，不对外发布）
 * @details 详见 phase-1-working-file.md §4.13。所有 C ABI 函数体必须用此宏包裹。
 */

#ifndef ZBASE_INTERNAL_C_API_GUARD_HPP_
#define ZBASE_INTERNAL_C_API_GUARD_HPP_

#include <exception>
#include <new>

#include "zbase/error.h"

#define ZBASE_C_API_BEGIN try {

#define ZBASE_C_API_END(default_err)                                  \
    } catch (const std::bad_alloc&) { return Z_ERR_NOMEM;          } \
      catch (const std::exception&)  { return (default_err);        } \
      catch (...)                    { return (default_err);        }

#endif  // ZBASE_INTERNAL_C_API_GUARD_HPP_
```

- [ ] **Step 9.2: 把各模块 C ABI 函数体用宏包裹**

以 `string.cpp` 中的 `z_string_split` 为例：

修改 `c:\Repos\ZBase\source\src\core\string\string.cpp` 顶部加入：
```cpp
#include "zbase/internal/c_api_guard.hpp"
```

把 `z_string_split` 函数体改为：

```cpp
int z_string_split(const char* input, size_t input_len,
                   const char* sep,
                   char*** out_arr, size_t* out_count) {
    ZBASE_C_API_BEGIN
    if (input == nullptr || sep == nullptr || out_arr == nullptr || out_count == nullptr) {
        return Z_ERR_INVALID_ARG;
    }
    // ... 原实现 ...
    return Z_OK;
    ZBASE_C_API_END(Z_ERR_UNKNOWN)
}
```

对所有返回 `int` 的 C ABI 函数（`z_string_*` / `z_file_*` / `z_time_format_iso` / `z_perf_*` / `z_log_init`）应用此模式。返回 `void` 的函数（`z_file_free` / `z_string_free` / `z_perf_dump` / `z_perf_reset` / `z_log_shutdown` / `z_log_write`）无需包裹（已是 noexcept 风格）。

- [ ] **Step 9.3: 跑全部测试确认无回归**

```powershell
cmake --build build
ctest --test-dir build --output-on-failure
```

Expected: 全部测试仍 PASS。

- [ ] **Step 9.4: 删除 dummy.cpp**

```powershell
Remove-Item c:\Repos\ZBase\source\src\platform\dummy.cpp
```

- [ ] **Step 9.5: Commit**

```powershell
git add source/include/zbase/internal/c_api_guard.hpp source/src/core/string/string.cpp source/src/core/file/file_reader.cpp source/src/core/file/file_writer.cpp source/src/core/file/dir_iter.cpp source/src/core/time/time.cpp source/src/core/perf/perf.cpp source/src/core/log/log.cpp
git rm source/src/platform/dummy.cpp
git commit -m "refactor(c_api): 引入 ZBASE_C_API_BEGIN/END 异常包裹宏并应用到所有 C ABI 函数"
```

**验收标准：**
- 全部单元测试无回归
- 用 `dumpbin /exports build\zbase.dll` 列出导出符号，确认导出函数数量 ≤ 30（YAGNI 守门）

---

## Task 10: C++ 包装层（header-only RAII）

**Files:**
- Create: `c:\Repos\ZBase\source\include\zbase++\error.hpp`
- Create: `c:\Repos\ZBase\source\include\zbase++\string.hpp`
- Create: `c:\Repos\ZBase\source\include\zbase++\time.hpp`
- Create: `c:\Repos\ZBase\source\include\zbase++\file.hpp`
- Create: `c:\Repos\ZBase\source\include\zbase++\perf.hpp`
- Create: `c:\Repos\ZBase\source\include\zbase++\log.hpp`

**Goal:** 提供基于 RAII 的 C++ 接口，调用 C ABI，异常时抛 `zbase::Exception`。

- [ ] **Step 10.1: 写 error.hpp**

Create `c:\Repos\ZBase\source\include\zbase++\error.hpp`:

```cpp
/**
 * @file error.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief C++ 异常 + 错误码工具
 */

#pragma once

#include <stdexcept>
#include <string>

#include "zbase/error.h"

namespace zbase {

/// ZBase 异常基类
class Exception : public std::runtime_error {
 public:
  explicit Exception(int err)
      : std::runtime_error(z_error_msg(err)), err_(err) {}
  Exception(int err, const std::string& extra)
      : std::runtime_error(std::string(z_error_msg(err)) + ": " + extra), err_(err) {}
  int code() const noexcept { return err_; }
 private:
  int err_;
};

/// 把错误码转为异常抛出（err != Z_OK 时抛）
inline void ThrowIfError(int err) {
  if (err != Z_OK) throw Exception(err);
}

}  // namespace zbase
```

- [ ] **Step 10.2: 写 string.hpp**

Create `c:\Repos\ZBase\source\include\zbase++\string.hpp`:

```cpp
/**
 * @file string.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 字符串工具 C++ 包装
 */

#pragma once

#include <string>
#include <vector>

#include "zbase/string.h"
#include "zbase++/error.hpp"

namespace zbase {

inline std::vector<std::string> StringSplit(const std::string& s, const std::string& sep) {
    char** arr = nullptr;
    size_t count = 0;
    int err = z_string_split(s.data(), s.size(), sep.c_str(), &arr, &count);
    ThrowIfError(err);
    std::vector<std::string> result;
    result.reserve(count);
    for (size_t i = 0; i < count; ++i) {
        result.emplace_back(arr[i] ? arr[i] : "");
    }
    z_string_free_array(arr, count);
    return result;
}

inline std::string StringJoin(const std::vector<std::string>& parts, const std::string& sep) {
    std::vector<char*> ptrs;
    ptrs.reserve(parts.size());
    for (const auto& p : parts) ptrs.push_back(const_cast<char*>(p.c_str()));
    char* out = nullptr;
    int err = z_string_join(ptrs.data(), ptrs.size(), sep.c_str(), &out);
    ThrowIfError(err);
    std::string result(out);
    z_string_free(out);
    return result;
}

inline std::string StringReplace(const std::string& s, const std::string& from, const std::string& to) {
    char* out = nullptr;
    int err = z_string_replace(s.c_str(), from.c_str(), to.c_str(), &out);
    ThrowIfError(err);
    std::string result(out);
    z_string_free(out);
    return result;
}

inline std::string StringTrim(const std::string& s) {
    char* out = nullptr;
    int err = z_string_trim(s.c_str(), &out);
    ThrowIfError(err);
    std::string result(out);
    z_string_free(out);
    return result;
}

inline std::string StringToLower(const std::string& s) {
    char* out = nullptr;
    int err = z_string_tolower(s.c_str(), &out);
    ThrowIfError(err);
    std::string result(out);
    z_string_free(out);
    return result;
}

inline std::string StringToUpper(const std::string& s) {
    char* out = nullptr;
    int err = z_string_toupper(s.c_str(), &out);
    ThrowIfError(err);
    std::string result(out);
    z_string_free(out);
    return result;
}

inline size_t StringUtf8Len(const std::string& s) {
    return z_string_utf8_len(s.c_str());
}

}  // namespace zbase
```

- [ ] **Step 10.3: 写 time.hpp**

Create `c:\Repos\ZBase\source\include\zbase++\time.hpp`:

```cpp
/**
 * @file time.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 时间工具 C++ 包装
 */

#pragma once

#include <cstdint>
#include <string>

#include "zbase/time.h"
#include "zbase++/error.hpp"

namespace zbase {

inline int64_t NowMs() { return z_time_now_ms(); }
inline int64_t NowUs() { return z_time_now_us(); }
inline void SleepMs(uint32_t ms) { z_time_sleep_ms(ms); }
inline void SleepUs(uint64_t us) { z_time_sleep_us(us); }

inline std::string FormatIso(int64_t ms) {
    char buf[32];
    int err = z_time_format_iso(ms, buf, sizeof(buf));
    ThrowIfError(err);
    return std::string(buf);
}

}  // namespace zbase
```

- [ ] **Step 10.4: 写 file.hpp**

Create `c:\Repos\ZBase\source\include\zbase++\file.hpp`:

```cpp
/**
 * @file file.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 文件 RAII 包装
 */

#pragma once

#include <string>
#include <string_view>
#include <vector>

#include "zbase/file.h"
#include "zbase++/error.hpp"

namespace zbase {

class FileReader {
 public:
  explicit FileReader(std::string_view path) {
    int err = z_file_read_text(std::string(path).c_str(), &buf_, &len_);
    ThrowIfError(err);
  }
  ~FileReader() { if (buf_) z_file_free(buf_); }
  FileReader(const FileReader&) = delete;
  FileReader& operator=(const FileReader&) = delete;
  FileReader(FileReader&& o) noexcept : buf_(o.buf_), len_(o.len_) { o.buf_ = nullptr; }
  FileReader& operator=(FileReader&& o) noexcept {
    if (this != &o) { if (buf_) z_file_free(buf_); buf_ = o.buf_; len_ = o.len_; o.buf_ = nullptr; }
    return *this;
  }
  [[nodiscard]] std::string_view View() const noexcept { return {buf_, len_}; }
  [[nodiscard]] std::string Str() const { return {buf_, len_}; }
  [[nodiscard]] size_t Size() const noexcept { return len_; }
 private:
  char*  buf_ = nullptr;  ///< 缓冲区指针
  size_t len_ = 0;        ///< 缓冲区长度
};

inline void WriteFile(std::string_view path, std::string_view content) {
    int err = z_file_write_text(std::string(path).c_str(),
                                content.data(), content.size());
    ThrowIfError(err);
}

inline bool FileExists(std::string_view path) {
    int r = z_file_exists(std::string(path).c_str());
    if (r < 0) ThrowIfError(r);
    return r == 1;
}

inline void MakeDirs(std::string_view path) {
    int err = z_file_mkdir(std::string(path).c_str());
    ThrowIfError(err);
}

/// 目录条目（值类型，next 后立即拷贝）
struct DirEntry {
    std::string name;
    bool        is_dir;
    uint64_t    size;
    uint64_t    mtime;
};

class DirIterator {
 public:
  explicit DirIterator(std::string_view path) {
    int err = z_dir_iter_create(std::string(path).c_str(), &iter_);
    ThrowIfError(err);
  }
  ~DirIterator() { if (iter_) z_dir_iter_destroy(iter_); }
  DirIterator(const DirIterator&) = delete;
  DirIterator& operator=(const DirIterator&) = delete;
  DirIterator(DirIterator&& o) noexcept : iter_(o.iter_) { o.iter_ = nullptr; }

  bool Next(DirEntry* out) {
    z_dir_iter_entry_t e;
    std::memset(&e, 0, sizeof(e));
    int err = z_dir_iter_next(iter_, &e);
    if (err == Z_ERR_NOT_FOUND) return false;
    ThrowIfError(err);
    out->name   = e.name ? e.name : "";
    out->is_dir = e.is_dir != 0;
    out->size   = e.size;
    out->mtime = e.mtime;
    return true;
  }
 private:
  z_dir_iter* iter_ = nullptr;
};

}  // namespace zbase
```

- [ ] **Step 10.5: 写 perf.hpp / log.hpp**

Create `c:\Repos\ZBase\source\include\zbase++\perf.hpp`:

```cpp
/**
 * @file perf.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief perf C++ 包装
 */

#pragma once

#include <string_view>

#include "zbase/perf.h"
#include "zbase++/error.hpp"

namespace zbase {

class PerfScope {
 public:
  explicit PerfScope(std::string_view name) : name_(name) {
    ThrowIfError(z_perf_start(name_.c_str()));
  }
  ~PerfScope() { z_perf_end(name_.c_str()); }
  PerfScope(const PerfScope&) = delete;
  PerfScope& operator=(const PerfScope&) = delete;
 private:
  std::string name_;
};

inline void PerfDump() { z_perf_dump(); }
inline void PerfReset() { z_perf_reset(); }

}  // namespace zbase

#define ZBASE_PERF_SCOPE(name) ::zbase::PerfScope _zbase_perf_##__LINE__(name)
```

Create `c:\Repos\ZBase\source\include\zbase++\log.hpp`:

```cpp
/**
 * @file log.hpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief log C++ 包装
 */

#pragma once

#include "zbase/log.h"

namespace zbase {

class Logger {
 public:
  explicit Logger(z_log_level_t level = Z_LOG_LEVEL_INFO) {
    z_log_init(level);
  }
  ~Logger() { z_log_shutdown(); }
  Logger(const Logger&) = delete;
  Logger& operator=(const Logger&) = delete;
};

}  // namespace zbase
```

- [ ] **Step 10.6: 写包装层冒烟测试（编译验证）**

Create `c:\Repos\ZBase\source\tests\core\test_cpp_wrapper.cpp`:

```cpp
/**
 * @file test_cpp_wrapper.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief C++ 包装层冒烟测试
 */

#include <gtest/gtest.h>
#include "zbase++/string.hpp"
#include "zbase++/file.hpp"
#include "zbase++/time.hpp"
#include "zbase++/perf.hpp"
#include "zbase++/log.hpp"

TEST(CppWrapper, StringSplit) {
    auto v = zbase::StringSplit("a,b,c", ",");
    EXPECT_EQ(v.size(), 3u);
    EXPECT_EQ(v[0], "a");
    EXPECT_EQ(v[2], "c");
}

TEST(CppWrapper, FileWriteReadRoundTrip) {
    const char* path = "zbase_cpp_test.txt";
    zbase::WriteFile(path, "hello cpp");
    zbase::FileReader r(path);
    EXPECT_EQ(r.Str(), "hello cpp");
    EXPECT_EQ(r.Size(), 9u);
    std::remove(path);
}

TEST(CppWrapper, PerfScope) {
    zbase::Logger logger(Z_LOG_LEVEL_DEBUG);
    {
        ZBASE_PERF_SCOPE("cpp_task");
        zbase::SleepMs(5);
    }
    zbase::PerfDump();
    SUCCEED();
}
```

- [ ] **Step 10.7: 跑测试通过**

```powershell
cmake --build build
ctest --test-dir build -R CppWrapper --output-on-failure
```

Expected: 3 个测试全 PASS。

- [ ] **Step 10.8: Commit**

```powershell
git add source/include/zbase++/ source/tests/core/test_cpp_wrapper.cpp
git commit -m "feat(cpp): 添加 header-only C++ RAII 包装层"
```

**验收标准：** 3 个 CppWrapper 测试全绿。

---

## Task 11: 集成测试 + 示例

**Files:**
- Create: `c:\Repos\ZBase\source\examples\demo.cpp`
- Create: `c:\Repos\ZBase\source\examples\CMakeLists.txt` (替换 Task 0 占位)
- Create: `c:\Repos\ZBase\source\tests\integration\test_demo.cpp`

**Goal:** 写一个端到端 demo（读文件 → 字符串处理 → perf 计时 → log 输出），并提供集成测试验证。

- [ ] **Step 11.1: 写 demo.cpp**

Create `c:\Repos\ZBase\source\examples\demo.cpp`:

```cpp
/**
 * @file demo.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief ZBase v0.1 综合示例：读文件 → 字符串处理 → perf 计时 → log 输出
 */

#include <cstdio>
#include <cstdlib>

#include "zbase++/log.hpp"
#include "zbase++/perf.hpp"
#include "zbase++/file.hpp"
#include "zbase++/string.hpp"
#include "zbase++/time.hpp"

int main(int argc, char** argv) {
    zbase::Logger logger(Z_LOG_LEVEL_DEBUG);

    Z_LOG_INFO("ZBase v%s 启动", z_version());

    // 准备测试文件
    const char* path = "zbase_demo_input.txt";
    zbase::WriteFile(path, "hello,world,你好,世界");

    try {
        ZBASE_PERF_SCOPE("demo_total");

        // 1. 读文件
        zbase::FileReader reader(path);
        Z_LOG_INFO("读到 %zu 字节", reader.Size());

        // 2. 字符串处理
        auto parts = zbase::StringSplit(reader.Str(), ",");
        Z_LOG_INFO("分隔得到 %zu 个片段", parts.size());

        // 3. 拼接回去
        std::string joined = zbase::StringJoin(parts, "|");
        Z_LOG_INFO("拼接结果: %s", joined.c_str());

        // 4. UTF-8 长度
        Z_LOG_INFO("UTF-8 字符数: %zu", zbase::StringUtf8Len(joined));
    } catch (const zbase::Exception& e) {
        Z_LOG_ERROR("ZBase 异常: %s (code=%d)", e.what(), e.code());
        return 1;
    }

    zbase::PerfDump();
    Z_LOG_INFO("当前时间: %s", zbase::FormatIso(zbase::NowMs()).c_str());

    std::remove(path);
    return 0;
}
```

- [ ] **Step 11.2: 写 examples/CMakeLists.txt**

Replace `c:\Repos\ZBase\source\examples\CMakeLists.txt`:

```cmake
file(GLOB_RECURSE ZBASE_EXAMPLE_SOURCES CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp"
)

foreach(src ${ZBASE_EXAMPLE_SOURCES})
    get_filename_component(name ${src} NAME_WE)
    add_executable(${name} ${src})
    target_link_libraries(${name} PRIVATE zbase)
endforeach()
```

- [ ] **Step 11.3: 写集成测试**

Create `c:\Repos\ZBase\source\tests\integration\test_demo.cpp`:

```cpp
/**
 * @file test_demo.cpp
 * @author 朱鹏 (Radica Zhu)
 * @date 2026-06-29
 * @version 0.1.0
 * @brief 端到端集成测试：验证 demo 完整流程
 */

#include <gtest/gtest.h>
#include "zbase++/log.hpp"
#include "zbase++/perf.hpp"
#include "zbase++/file.hpp"
#include "zbase++/string.hpp"
#include "zbase++/time.hpp"

TEST(IntegrationDemo, FullPipeline) {
    zbase::Logger logger(Z_LOG_LEVEL_DEBUG);

    const char* path = "zbase_integration_test.txt";
    zbase::WriteFile(path, "alpha,beta,γ,δ");

    {
        ZBASE_PERF_SCOPE("integration_total");
        zbase::FileReader reader(path);
        EXPECT_EQ(reader.Size(), 14u);

        auto parts = zbase::StringSplit(reader.Str(), ",");
        EXPECT_EQ(parts.size(), 4u);
        EXPECT_EQ(parts[0], "alpha");
        EXPECT_EQ(parts[2], "γ");

        std::string joined = zbase::StringJoin(parts, "+");
        EXPECT_EQ(joined, "alpha+beta+γ+δ");

        EXPECT_EQ(zbase::StringUtf8Len("γδ"), 2u);
    }

    zbase::PerfDump();
    zbase::PerfReset();
    std::remove(path);
}
```

- [ ] **Step 11.4: 构建 + 跑 demo + 跑集成测试**

```powershell
cmake --build build
.\build\demo.exe
ctest --test-dir build -R IntegrationDemo --output-on-failure
```

Expected:
- demo 输出 INFO 日志 + perf dump + ISO 时间
- 集成测试 1 个 PASS

- [ ] **Step 11.5: Commit**

```powershell
git add source/examples/demo.cpp source/examples/CMakeLists.txt source/tests/integration/test_demo.cpp
git commit -m "feat(examples): 添加端到端 demo 与集成测试"
```

**验收标准：**
- `demo.exe` 输出中文日志不乱码
- 集成测试 PASS

---

## Task 12: 四平台烟测

**Files:** 无（本 Task 是验证性回归）

**Goal:** 在 4 套工具链 (Win MSVC / Win MinGW / Linux GCC / macOS Clang) 上重新构建 + 跑全部测试，确认零回归。

- [ ] **Step 12.1: 清空 build 缓存**

```powershell
Remove-Item -Recurse -Force c:\Repos\ZBase\source\build
Remove-Item -Recurse -Force c:\Repos\ZBase\source\build-mingw
```

- [ ] **Step 12.2: Windows MSVC 全量构建**

```powershell
cd c:\Repos\ZBase\source
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
ctest --test-dir build --output-on-failure
```

Expected: 全部测试 PASS，无警告无错误。

- [ ] **Step 12.3: Windows MinGW 全量构建**

```powershell
cd c:\Repos\ZBase\source
cmake -B build-mingw -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++
cmake --build build-mingw
ctest --test-dir build-mingw --output-on-failure
```

Expected: 全部测试 PASS。

- [ ] **Step 12.4: Linux GCC 全量构建**

> Linux 平台需要在 Linux 机器或 WSL 中执行。

```bash
cd /path/to/ZBase/source
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
ctest --test-dir build --output-on-failure
```

Expected: 全部测试 PASS。

- [ ] **Step 12.5: macOS Clang 全量构建**

> macOS 平台需要在 macOS 机器上执行。

```bash
cd /path/to/ZBase/source
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
ctest --test-dir build --output-on-failure
```

Expected: 全部测试 PASS。

- [ ] **Step 12.6: 符号导出审计**

```powershell
# Windows MSVC
dumpbin /exports build\zbase.dll | findstr "z_"
```

Expected: 导出符号 ≤ 30 个（YAGNI 守门），全部以 `z_` 开头。

```bash
# Linux/macOS
nm -D --defined-only libzbase.so | awk '$2=="T"{print $3}' | sort
```

Expected: 同上。

- [ ] **Step 12.7: 静态库模式烟测**

```powershell
Remove-Item -Recurse -Force c:\Repos\ZBase\source\build-static
cd c:\Repos\ZBase\source
cmake -B build-static -G Ninja -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF
cmake --build build-static
ctest --test-dir build-static --output-on-failure
```

Expected: 静态库模式下全部测试 PASS，`ZBASE_API` 宏展开为空。

- [ ] **Step 12.8: install 烟测**

```powershell
cmake --install build --prefix c:\Repos\ZBase\source\install-test
```

Expected: install 目录含 `bin/zbase.dll`、`lib/zbase.lib`、`include/zbase/*.h`、`lib/cmake/ZBase/ZBaseConfig.cmake`。

- [ ] **Step 12.9: 更新 phase-1-working-file.md 进度表**

把所有 Step 状态从 ⬜ 未开始 → ✅ 完成。

- [ ] **Step 12.10: 归档工作文件**

```powershell
cd c:\Repos\ZBase\source\docs
git mv phase-1-working-file.md phase-1-done.md
```

- [ ] **Step 12.11: 最终 Commit**

```powershell
git add source/docs/phase-1-done.md source/docs/phase-1-working-file.md
git commit -m "docs: ZBase v0.1 完成，归档 phase-1 工作文件"
git tag v0.1.0
```

**验收标准：**
- 4 套工具链全绿
- 静态库模式全绿
- install 产物完整
- 导出符号 ≤ 30
- 工作文件归档

---

## Self-Review

### 1. Spec Coverage

逐条对照 [phase-1-working-file.md](file:///c:/Repos/ZBase/source/docs/phase-1-working-file.md) §1.1 P0 交付：

| # | 交付项 | 对应 Task |
|---|--------|-----------|
| 1 | CMake 跨平台骨架 | Task 0 |
| 2 | 平台层 encoding + file/time | Task 1, 4 |
| 3 | error 模块（9 错误码 + 中文描述） | Task 2 |
| 4 | string（split/join/replace/trim/tolower/toupper/utf8_len） | Task 3 |
| 5 | time（now_ms/now_us/format_iso/sleep） | Task 5 |
| 6 | file（read_text/write_text/exists/mkdir/dir_iter） | Task 6 |
| 7 | perf（start/end/dump + Z_PERF_SCOPE） | Task 7 |
| 8 | log（4 级 + stderr + UTF-8） | Task 8 |
| 9 | C ABI 导出（统一异常包裹） | Task 9 |
| 10 | C++ RAII 包装 | Task 10 |
| 11 | GoogleTest + 各模块单测 | Task 2-10 |
| 12 | 综合 demo | Task 11 |
| 13 | 四平台烟测 | Task 12 |

phase-1-working-file.md §4 全部 29 条注意事项（坑位）均已映射到对应 Task 的代码实现中：

| 坑位 | 落地 Task |
|------|-----------|
| §4.1 GLOB_RECURSE + CONFIGURE_DEPENDS | Task 0 |
| §4.2 /utf-8 + UTF-8 无 BOM | Task 0 |
| §4.3 中文路径走 W 版 API | Task 1, 4, 6 |
| §4.4 /permissive- /W4 | Task 0 |
| §4.5 MinGW C++20 兼容 | Task 0, 4 |
| §4.6 库申请库释放 | Task 3, 6, 10 |
| §4.7 异常不逃逸 C ABI | Task 9 |
| §4.8 显式 init/shutdown | Task 8 (log) |
| §4.9 GoogleTest 集成 | Task 0, 1 |
| §4.10 doxygen 文件头 | All |
| §4.11 路径分隔符 `/` | Task 4 |
| §4.12 QPC / clock_gettime | Task 4 |
| §4.13 ZBASE_C_API_BEGIN/END | Task 9 |
| §4.14 CRT 链接约定 | Task 0 |
| §4.15 公共头禁用类型 | Task 0 (静态规则) + CI |
| §4.16 install 规则 | Task 0 |
| §4.17 RPATH/soname | Task 0 |
| §4.18 静态库双模式 | Task 0 |
| §4.19 POD reserved[8] | Task 6 |
| §4.20 _v2 命名 | Task 2-10 设计约束 |
| §4.21 控制台 save/restore | Task 8 |
| §4.22 二进制模式不转换换行 | Task 6 |
| §4.23 MAX_PATH \\?\ 前缀 | Task 4 |
| §4.24 free nullptr 安全 | Task 3, 6 |
| §4.25 符号导出 CI 检查 | Task 12 |
| §4.26 SetDefaultDllDirectories | Task 8 |
| §4.27 enum + static_assert | Task 2 |
| §4.28 YAGNI 守门 ≤ 30 | Task 12 审计 |
| §4.29 thread_local 不用 | All |

### 2. Placeholder Scan

无 TBD / TODO / 占位符。所有代码段都是可直接使用的完整实现。

### 3. Type Consistency

- `z_error_t` 在 Task 2 定义，Task 3-10 中调用名一致 ✓
- `z_dir_iter_entry_t` 在 Task 6 定义，Task 6 测试与 Task 10 包装中字段名（`name`/`is_dir`/`size`/`mtime`/`reserved`）一致 ✓
- `zbase::Exception` 在 Task 10 error.hpp 定义，string.hpp/file.hpp/perf.hpp 中 `ThrowIfError` 调用一致 ✓
- `Accumulator` 在 Task 7 perf.cpp 内部定义 ✓

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-29-zbase-v0.1.md`. Two execution options:

**1. Subagent-Driven (recommended)** - 每个 Task 派一个独立 subagent 实现，主会话只做调度与 review，互不污染上下文，迭代速度快。

**2. Inline Execution** - 在当前会话里按 Task 顺序逐步执行，每个 Task 走写测试 → 失败 → 实现 → 通过 → commit 循环。

请选择执行方式。
